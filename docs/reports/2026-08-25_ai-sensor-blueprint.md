# AI 传感器链条蓝图 B0（用户愿景固化 + 学术评估，2026-08-25）

> 状态：**规划中——先规划不实施**（沿用 ROADMAP 愿景范畴声明的纪律，本报告
> 将其从「一句话愿景」升级为「结构化规划+待裁决项」）。用户原话见 §1；
> 学术评估见 §3；五项待用户裁决见 §4。实施排期在 P7 主线收官后。

## 1. 用户愿景固化（2026-08-25 口述，原意保全）

**① 工作方式**：zcode 调用工具 → 链接软件接口 → 接口连接结构性语料与全文，
查看尺度让 AI 自由切换。AI 工作存在**读文献**与**梳理领域**两个模式：
每篇文献由三个孙智能体独立负责——deepseek 一读、GLM 二读、GLM 思辨裁决；
AI 在文章中寻找论据并标注（锚定原文），输出的判断作为笔记保存——**同一段
话可以看到三段 AI 语料**；AI 语料与用户人工语料共同构成提纯后的高质量语料，
供负责梳理领域的子智能体阅读判断；梳理子智能体由 zcode 调用，负责绘制发展
脉络；zcode 任务本体阅读整个领域的发展脉络与关键点、派出子孙智能体获取
可靠信息、直接与用户对接。

**② 读法模板（主问题+派生问题）**：一篇文章对同行最大的价值是什么？凭什么
发顶刊/一区？核心 idea 在哪里？未被记录的工程债务可能在哪里？全文中哪个
片段最符合你对自然科学的品味？——「深刻≠复杂：越能迁移复用、信息结构越
普遍存在、越能从多角度交叉印证，就越深刻有趣，越能改变人们对某一问题的
理解」。按固定模板带着批判性思维阅读；可联网搜索作者及通讯作者履历辅助
判断「为什么是这个人做出来了」「认真做还是灌水」。

**③ 总述**：制定专业流程和工具，让 AI 拿着工具说明书自由查看结构化信息
然后输出——这是 AI 应用在文献阅读领域最务实的方向。

## 2. 架构分解（愿景的工程转译）

```
用户 ⇄ zcode（编排者：对接用户/阅读脉络/派发任务）
         │ 调用（文件系统级：读语料目录三件套——ADR-0011）
         ▼
   领域梳理子智能体（读全部语料 → 绘制发展脉络 md）
         │ 消费（AI 语料 ∥ 用户语料）
         ▼
   语料池（corpus/*.md：WADM 锚定引文块 + [user]/[ai:*] 作者标记）
         ▲ 产出（三段 AI 语料锚定同一段原文）
   三孙智能体/篇：deepseek 一读 → GLM 二读 → GLM 思辨裁决
         │ 输入（工具说明书 INTERFACE.md + 全文 app-file:// + 读法模板）
```

关键复用：本项目最强资产=WADM 锚定（quote/prefix/suffix/rects）——AI 论据
锚定原文即幻觉接地（grounding）的工程解；语料导出（ADR-0011）即传感器接口。

## 3. 学术评估

### 3.1 方法论优势（可成立的判断）

1. **异构多读+裁决的结构合理**：单模型评文献有系统性偏差（自我偏好/长度
   偏好，LLM-as-judge 文献共识）；deepseek+GLM 属不同模型家族，其错误相关性
   低于同模型温度采样——「多裁判+裁决」优于「单模型三次采样」。
2. **锚定式输出是本项目相对通用 AI 阅读器的差异化**：判断必须挂在引文块上
   （可回溯一级证据），这把 AI 输出从「自由文本」约束为「证据引用型语料」。
3. **品味判据可操作化**：「迁移复用性/信息结构普遍性/交叉印证角度」可注入
   提示词的操作性判据，非空话——且与信息论视角一致（高迁移性=低冗余编码）。
4. **工具说明书驱动正确**：AI 能力随模型迭代，应用侧只做「把数据说明白」
   （INTERFACE.md），不做提示词/流程的深度耦合——传感器定位务实。

### 3.2 风险与修正建议（学术审慎项）

1. **成本/价值比未分层**：三孙智能体×全文长上下文+履历搜索，单篇成本可观；
   学术工作流中多数文献只需浅筛（摘要+图表），少数值得深读。**建议加筛选层**
   （单模型廉价 triage → 用户或规则标记深读集 → 才触发三读）——见 D1。
2. **裁决者与二读同族（GLM）**：共享训练分布的偏差相关性削弱「独立裁决」；
   但换异构模型=第四方依赖/成本。**可辩护条件**：裁决者只读一读二读的
   分歧点+原文锚定段（不重读全文），角色差异（批判框架）替代模型差异——
   见 D2。
3. **「提纯后高质量语料」假设过强**：三模型共识≠正确（共识性错误仍可能）；
   梳理智能体读 AI 语料=二阶摘要链（原文→三读→语料→脉络，每层有损）。
   **对策**：脉络结论必须引用语料中的引文块（二级回溯到原文），梳理输出
   的每个论断挂 corpus 文件+引文位置——把有损链约束成可审计链。
4. **履历搜索的信效度与安全面**：「认真还是灌水」是基于公开信息的动机推断，
   错误结论的代价高（对学者的诽谤性判断）；且需新增出网 host（安全禁令：
   白名单外新增需 ADR+[locked-change]）。**建议收窄为「语境信息」**（学术
   谱系/机构/时间线——回答「为什么是这个人」），动机评判留给用户——见 D4。
5. **「绘制发展脉络」的表达形态**：与负面清单「不做网络图可视化」裁决的
   边界须一致——建议形态=md 时间线文档+阶段主题聚类（标签承载），线性
   可读——见 D5。
6. **AI 语料的数据模型缺口**：annotations.comment 是单字符串，三段 AI 语料
   塞同一字段会污染用户批注面；001 冻结只能追加迁移。两个方案：加
   `author` 列（扩展既有表）vs 独立 ai_notes 表——影响 P7-C 导出面与
   ADR-0011 v2——见 D3（P7-C 工单化前必须裁决）。
7. **智能体编排的宿主问题**：zcode 调用子/孙智能体的机制（会话内并行
   Agent？外部脚本？）属 zcode 侧能力，应用侧只需保证接口自包含
   （INTERFACE.md+全文件可读）——应用边界清晰是优势，但「三孙智能体读
   全文」需要 PDF 文本层导出（语料目录加 `fulltext/<paperId>.txt`？）——
   接口扩展项，随 D3 一并裁决。

### 3.3 定位判断（回应「最务实方向」）

同意务实——理由三条：接口侧轻（文件系统+说明书，无服务化）；智能侧外置
（模型迭代不牵动应用）；锚定复用（既有资产直接变现）。**但务实的边界**：
AI 读 PDF 的尺度切换（跳转页码/看图）在纯 md 语料下不可得（图不可语料化、
表格失真）——v1 接受「文本层+引文锚定」的降级读法，图文混读留待接口 v2
（如导出页图快照目录）。

## 4. 待用户裁决（D1-D5，裁决后入 ADR/工单化）

- **D1 分层读法**：是否加筛选层（单模型 triage→深读集→三读），还是全量
  三读（成本高但无漏筛）？建议：分层。
- **D2 裁决者构成**：GLM 思辨裁决（同族取舍）是否接受？建议：接受+「只读
  分歧点与锚定段」的聚焦裁决（成本与独立性双收）；若坚持异构，候选=第四
  模型或用户人工裁决终审位。
- **D3 AI 语料数据模型**：annotations 加 author 列 vs 独立 ai_notes 表？
  建议：**独立表**（不污染既有批注语义与 FTS 联动；迁移 00x 追加合法；
  导出面 ADR-0011 v2 统一装配 [ai:*] 段）。此裁决是 P7-C 工单化的前置。
- **D4 履历搜索定位**：语境信息（谱系/时间线）vs 动机评判（灌水判定）？
  建议语境化+评判留用户；出网 host 需求随之定（arXiv/CrossRef/OpenAlex
  现白名单已含作者履历的大部分元数据——可能零新增 host，工单化时核实）。
- **D5 脉络形态**：md 时间线+标签聚类（负面清单边界内）确认？

## 4.1 裁决记录（2026-08-25 用户逐项裁决，同日第二轮）

| # | 裁决 | 用户理由/附加信息 | 工程含义（评估确认项） |
| --- | --- | --- | --- |
| D1 | **全量三读**（否决分层） | 读完即建档、此后不碰基础文本——要数据基座尽可能不失真 | 一次性数字化成本可接受；三护栏：队列化+断点续跑（按篇幂等）/进度入 manifest/分批手动触发（与 D2 激活语义自洽） |
| D2 | 可审计链认同；**用户笔记计入高质量语料** | —— | 用户语料=异质信号源：人机分歧点是高价值信息，裁决者与梳理智能体应显式消费分歧而非只看一致（本表新增规约） |
| D2b | **模型可配置**（一审/二审/裁决任配）；**工具安装到 zcode** 便于配置；应用启动后**手动激活** | —— | 应用侧零 AI 配置 UI、零 LLM 出网（模型调用全在 zcode 侧——安全面不动，显式声明）；激活=zcode 工具检测语料目录就绪，应用不自动触发 |
| D3 | **独立表**（按评估建议）；AI 消费=被标记的原文+AI 与人类笔记（DB 真相源+md 投影确认） | 孙智能体（一阶）吃全文+图；梳理智能体（二阶）吃标记+笔记语料——同目录不同子集 | P7-C 工单化前置已解除；表结构随工单规约 |
| D4 | **加含金量指标**（「真正牛逼的工作不会被埋没」） | 动机审判→代理度量 | cited_by_count（OpenAlex/CrossRef 现白名单可得，零新增 host）；三偏倚显式处理：领域基线归一/自引剔除或单列/venue 等级表=内置人工先验可改 |
| D5 | 线性时间线多路推进确认；**只抓主要矛盾=四类核心贡献枚举**（新定义/新指标/新方法/新策略）；不硬核或松散部分**用户手动设置/拖拽/标记** | —— | 四类枚举（definition/metric/method/strategy）固化进梳理智能体规约；「拖拽标记」引入轻量脉络编辑面——新 UI 面记 P8+ 工单化独立决策项（v1 可 md 直接编辑兜底；线性编辑器不违「不做网络图」裁决）。**（2026-08-25 §4.3 E3/E5 修订：形态升格为交互式时间树并提前立项 P7-H；四类枚举与多路推进语义不变）** |
| D6（新） | **图提取纳入接口 v1** | 为多模态模型考虑 | 两级：页级快照（pdf.js canvas→PNG）+标注区域裁剪（WADM rects 归一化可直接裁）；对象级 XObject 提取不做——契约修订见 ADR-0011 |

## 4.2 读法模板固化（2026-08-25 第三轮：七问 schema + ARA 对标）

### 学术对标：Agent-Native Research Artifact（ARA）潮流

2026-05 arXiv:2604.24658（Jiachen Liu 等 37 人，「The Last Human-Written
Paper」，GitHub: ARA-Labs）提出 AI-native 论文格式，两大诊断：

- **叙事税（storytelling tax）**：成文论文只保留约 20% 信息——过程、决策、
  失败尝试全部丢失；
- **工程税（engineering tax）**：论文是研究的**有损压缩**——实现细节缺失
  导致不可复现（其方案=生产侧自动记录全流程+形式化验证层）。

**对偶关系**：ARA 是**生产侧**的无损化（作者写作时保留幕后）；本链线的三读
建档是**消费侧的去税重建**——对既有的、已交税的 PDF 存量，由孙智能体从
成文逆向反推试错点/失败数据缺失位置。两者互补而非竞争：存量文献永远
等不到 ARA 改写，消费侧重建是唯一路径。Q3（工程债务）即该思想在提问层的
落地。

### 七问 schema（每段 AI 语料的固定分节；Q1/Q5 锚定强制）

| # | 问题 | 输出形态 |
| --- | --- | --- |
| Q1 | 核心 idea 是什么 | 必答；**必须挂引文块**（定义/命题原文处） |
| Q2 | 对同行的价值（改变了认知方式？开创范式大幅加快计算？解决工程问题？） | 必答；封闭枚举多标：认知改变/范式开创/速度数量级/工程落地/工具基建 |
| Q3 | 工程债务：失败数据未记录处、潜在试错点（ARA 叙事税的逆向重建） | 推断标注+缺失位置说明（不强求原文锚定——缺失物本就无原文） |
| Q4 | 学术谱系：为什么是这个单位、这个学生/作者？师承何方、祖传资源积累 | 定性语境（与 D4 含金量指标的定量面互补）；履历元数据现白名单可得 |
| Q5 | 全文哪个片段最符合自然科学品味（深刻≠复杂：可迁移/结构普遍/可交叉印证） | 必答；**必须挂引文块**（无锚定即无效输出） |
| Q6 | 未声明的局限与适用边界 | 两列：作者承认的 ∥ AI 识别的（审稿人视角） |
| Q7 | 验证强度 | 三轴评级：证明完备性/消融充分性/统计功效（与 Q3 相邻但独立——Q3 是「没写的」，Q7 是「写了但弱的」） |

裁决者（第三孙智能体）另加**分歧报告**节：一读二读的不一致点+裁决依据。
用户已授权「问题还可以加」——七问为 v1 冻结版，后续按实际读感迭代
（schema 演进规则同 ADR-0011）。

### 4.3 增量裁决（2026-08-25 第四轮：业务功能重述核实 E1~E7 + 联动新需求 N1~N4）

> 核实矩阵与四方案对比=docs/reports/2026-08-25_ai-rescope-verification.md（其 §5
> 推荐包经用户逐项批准）；契约落档=ADR-0014（lineage）/ADR-0015（回灌与伴随进程）；
> 编排=ROADMAP（P7-C 增补 N1、P7-G 增容 SR2-AI-06~10、P7-H 新节）。

**E1~E7 裁决**：

| # | 裁决 | 工程含义 |
| --- | --- | --- |
| E1 | AI 调用发起点=**B' 伴随进程+文件协议** | 应用写 job 请求文件→zcode 侧工具（用户手动拉起）拾取执行三读→产物落 corpus-ai/→应用轮询状态+导入。应用零 LLM 出网/零新 host/零密钥面（INV-08 不动，D2b 保持）；D utilityProcess 自含方案记 P8+ 升级候选（「zcode 不在跑也要能读」成为硬需求时再立项） |
| E2 | 回灌=**文件导入器** | corpus-ai/<paperId>.json（行式锚定段）→应用 ai-notes/import IPC→写 ai_notes（「写入只经应用 IPC」D3 语义保持，工具永不写 DB）；幂等（已导入篇去重） |
| E3 | 脉络图=**v1 时间树（单父）+v2 跨支 DAG** | y=年份分层+x=Reingold-Tilford tidy tree（零依赖手写，D3 库禁引）；梳理智能体输出从 md 时间线改为 lineage JSON 草稿；不做 md 中间形态（防两套方案并存红线） |
| E4 | 图归属=**新顶层视图「脉络」** | App 导航第四项；跨文献全局面语义 |
| E5 | 负面清单边界重裁 | 人工策展（AI 起草+人工修订）核心 idea 时间树合法；**自动引文网络图可视化维持不做**；ADR-0012 维持暂不做（复审条件触发但对象辨析不同——lineage 边=策展边非自动引文边），lineage 另立 ADR-0014 |
| E6 | 排序 | P7-B 收官→P7-C（含 N1）→AI-01~05（基座不变）→AI-06~10（回灌联动组）→LG-01~05（脉络组）；P7-F 可穿插（锚点定位服务按 F-aware 接口设计） |
| E7 | 密钥留工具侧 | 模型 API key 留 tools/ai-sensor/config.json（gitignore）；应用零密钥面（E1 选 A/D 才需应用侧方案，已否） |

**N1~N4 联动新需求**：

| # | 需求（用户原述） | 工程含义 |
| --- | --- | --- |
| N1 | 阅读器笔记↔标注双向单击定位（标注单击采方案a=菜单+侧栏同步） | 侧栏笔记条目单击→阅读器定位标注（滚动+闪烁）；标注单击→既有四选项菜单+侧栏同步高亮滚动。交付共享「锚点定位服务」（INV-20 三层防线单入口）；排期=P7-C 增补（工单化并入 C 组或紧随小单，工程自决） |
| N2 | AI 笔记=标注-内容成对（渲染对等/存储独立） | 渲染对等=AI 锚定段经 verifyQuote 重锚入标注层**同一几何管线**（七问分色单源，annotation-style 同族）；存储独立=永不写 annotations 表；**ai_notes 一行一锚定段+question 列**（Q1~Q7/divergence；锚可空=篇级回答，Q3/Q6/Q7 允许无锚）；AI 标注 v1 只读（无编辑/删除写路径）；三层防线（重锚→anchor_page 页级降级→仅开篇）升格验收条款+INV-20；DDL 修订并入 SR2-AI-01 |
| N3 | 脉络图节点单击详情、笔记双击跳阅读器 | 节点单击=侧板（元信息+核心 idea+AI/人工笔记，复用 ai-notes/list+notes 读）；笔记双击=锚点定位服务跳转（与 N1 共享 INV-20 单入口）；落点=P7-H SR2-LG-04 |
| N4 | 设置页 zcode 联动按钮（发现+一键装技能+心跳状态，不代启会话） | 状态三档（未发现 zcode/已装技能未运行/运行中——心跳文件新鲜度）；一键装技能=fs 复制应用资源内技能模板至 zcode 技能目录（点击+确认）；**不代启会话=按钮永不 spawn 进程**（INV-21，e2e 断言）；落点=SR2-AI-10 |

## 5. 实施边界声明

- 本报告=规划固化，不含任何实现承诺；实施前置条件=P7 主线收官（B/F/C）
  +出网/host 面安全审计（D4 已核实零新增）。
- 应用侧工程面（预期）：语料目录导出扩展（fulltext/figures/AI 段装配）+
  含金量指标导出（manifest 元信息）——均在既有架构模式内（导出器/对话框/
  INV-07）；AI 配置/编排/模型调用零应用侧负担（D2b）。

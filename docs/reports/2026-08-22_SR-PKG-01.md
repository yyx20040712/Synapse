# SR-PKG-01 审计报告：electron-builder NSIS 打包配置与 dist 编排

- 日期：2026-08-22
- 工单：SR-PKG-01（electron-builder NSIS 打包配置与 dist 编排）
- 规约：electron-builder.yml 与 scripts/dist.mjs 文件头五层注释（开单提交 f5c0eda，双模型审计 PASS）
- 结论：**PASS**。一审 PASS（2 NIT），二审终裁 PASS（独立复跑全部验收证据属实）；四条 NIT 均确认，均不构成回修义务。

## 1. 变更明细（5 个文件）

### 1.1 electron-builder.yml（新建）

- appId io.github.yyx20040712.synapse；productName Synapse Remake。
- directories.output=dist（已 gitignore）。
- files：out/** + package.json；排除 react、react-dom、zustand、pdfjs-dist（纯渲染依赖，renderer 由 vite 自足）与 better-sqlite3、abi-cache（双 ABI 缓存约 10MB）。
- asar 开启；asarUnpack '**/*.node'。
- electronDist=node_modules/electron/dist（本地复用，免二次下载）。
- npmRebuild=false + buildDependenciesFromSource=false（绑定已预置，禁源码重编译）。
- win：icon=build/icon.ico，target=nsis。
- nsis：oneClick=false、perMachine=false、allowToChangeInstallationDirectory=true、deleteAppDataOnUninstall=false、artifactName=Synapse-Remake-${version}-setup.${ext}。

### 1.2 scripts/dist.mjs（受锁文件，unlock→改→apply）

- SKIP_MIRROR != 1 时设 ELECTRON_BUILDER_BINARIES_MIRROR 与 ELECTRON_MIRROR（均指向 npmmirror）。
- 先跑 npm run build（spawnSync shell:true——Windows 下 npm.cmd 必需，参数全字面量）。
- 再以 node 启动按 bin 字段定位的 electron-builder cli，参数 --win nsis；stdio inherit。
- 任一步失败：打印步骤名并透传退出码（r.status ?? 1）。

### 1.3 package.json

- 新增 dist script 与 author（yyx20040712）。
- dependencies / devDependencies 零变动（无 [dep-change]）。

### 1.4 build/icon.ico（新建）

- PIL 生成六尺寸（256~16）圆角方块：#2563eb 底 + 白色 S。
- 10160 字节；ICO 头 00 00 01 00 校验通过。

### 1.5 locks/manifest.json

- dist.mjs 的 sha256 配套重算；85 条记录一致。

## 2. 验收证据（实现方执行 + 二审独立复跑均属实）

- npm run dist exit 0：产出 dist/Synapse-Remake-0.1.0-setup.exe（113,520,906 字节）与 dist/win-unpacked/。
- asar 断言：out/main/index.js、out/preload/index.cjs、out/renderer/index.html、better_sqlite3.node 在包内；abi-cache、react、react-dom、zustand、pdfjs-dist 零命中；zod 533 条目保留（main 运行时需要）。
- 绑定身份：app.asar.unpacked 内 better_sqlite3.node sha256=24e2e3ca…5d9b8，与 abi-cache/electron-v146 完全一致；node-v137 版本（e75b8c02…）与之不同——二审独立复核。
- 排除守卫：src/main 与 src/preload 对四个排除包的实际 import 为 0 处。
- 打包应用启动：SYNAPSE_USER_DATA 指向临时目录，前台存活 12 秒，synapse.db / -wal / -shm 生成（electron-v146 绑定在打包环境加载，数据库初始化成功）。
- npm run verify exit 0（212 测试 + 全关卡）。

## 3. 审计轨迹与 NIT 存档

- 一审（deepseek-v4-flash）：PASS，2 NIT。
- 二审终裁（GLM5.3 code-reviewer）：PASS。独立复跑全部验收证据；五硬约束逐条实测；sha256 独立重算；依赖红线零变动确认。
- 四条 NIT 裁决去向（均确认属实，均不构成回修义务）：
  1. yml 文化层「完成后」流程句在任务完成后仍保留——按二审建议提交时顺手改为完成时态（已落实）。
  2. files 未排除 react 的传递依赖 scheduler——存档为体积优化项。
  3. dist.mjs 失败分支未打印 spawnSync 的 r.error（诊断信息不足）——存档为改进项。
  4. asar 内残留 better-sqlite3 安装期依赖树约 33 个小包（prebuild-install、tar-fs、node-abi 等）——与第 2 条一并存档为体积优化项。
- 体积优化修法（留待需要时实施，本次不做）：files 改为排除 '!node_modules/**' 后显式加回 better-sqlite3 与 zod，并重跑打包验收。

## 4. 新机器首跑注意事项

- 首跑 npm run dist 时，winCodeSign-2.6.0.7z 因含 darwin symlink，无管理员权限解压失败。标准解法：预置 %LOCALAPPDATA%\electron-builder\Cache\winCodeSign\winCodeSign-2.6.0 目录（symlink 桩替换为实文件）。
- NSIS 3.0.4.1 与 nsis-resources 3.4.1 经 npmmirror 下载成功。
- 以上均为本机一次性操作，不进仓库；新机器首跑需同类处理。

## 5. 可复现命令

```bash
npm run dist
npx asar list dist/win-unpacked/resources/app.asar
# 断言：out/** 与 better_sqlite3.node 在包；四个排除包与 abi-cache 零命中
# sha256 对照（Git Bash）：以下两值应一致，且应异于 abi-cache/node-v137 的 e75b8c02…
sha256sum dist/win-unpacked/resources/app.asar.unpacked/node_modules/better-sqlite3/build/Release/better_sqlite3.node
sha256sum <abi-cache/electron-v146 内对应 better_sqlite3.node>
npm run verify
```

## 6. 提交信息（计划）

单 commit：5 个变更文件 + registry 翻状态（SR-PKG-01 open→done）+ 本报告；尾注 [locked-change]（dist.mjs 与 locks/manifest.json 受锁，manifest 已与本次变更同步）。

```
feat(dist): SR-PKG-01 electron-builder NSIS 打包配置与 dist 编排

- electron-builder.yml / scripts/dist.mjs / package.json / build/icon.ico / locks/manifest.json
- registry SR-PKG-01 open→done；附审计报告 docs/reports/2026-08-22_SR-PKG-01.md

[locked-change] scripts/dist.mjs 与 locks/manifest.json 受锁变更（unlock→改→apply，manifest 已同步）
```

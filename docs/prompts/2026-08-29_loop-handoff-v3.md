# 2026-08-29 LOOP 交接文档 v3——F1 划选役收官+用户四裁决落册+U2/U3/U4 开工书

> 定位：v2 交接后本会话完成 **U1=F1 划选修正役全链收官**（SR2-F-08 三屋
> 全流程+SR2-F-09 用户令改灰压缩票），用户**复测前提前拍板四项结构决策**
> （本文 §2——U2/U3 票面由此直接可开）。F1 根因与修法全档=
> `scripts/audits/f1-campaign-close.md`+`f1-forensics.report.md`+ADR-0019。
> 纪律总纲：全部三屋模式；U2 含数据模型面（U2b）先票面裁决后动工。

## 1. 开工自检（新会话第一动作）

1. **技能清点**：systematic-debugging（遇缺陷取证）/ TDD /
   verification-before-completion / subagent-driven-development 按单元加载；
   **本环境视觉通道事实**：Read/子代理 Read 图像均只回 CDN URL、
   analyze_image 对 CDN 签名 URL 400——视觉取证走「无阈值像素差分+
   DOM 状态转储」替代（f1-forensics5.mjs 系列即现成配方）。
2. **环境铁律**：node24 前缀=**`export PATH="/d/nodejs24:$PATH"`**（本机
   D:\nodejs24 v24.20.0——v2 文档里旧机路径已失效）；Git Bash 乱码≠损坏；
   真机取证走 playwright `_electron.launch`（out/main/index.js）+
   SYNAPSE_USER_DATA 指向真实库**副本**；**取证器三戒**（f1 战役实录）：
   拖拽进行中禁 Page.screenshot（打断 CDP 输入会话）、页内 Promise 必须
   6s 兜底（渲染冻结时 evaluate 无界挂）、结束必 app.close() 兜底。
3. **基线锚**：HEAD=本文档提交；verify=**104 文件 858 用例 / locks 161 /
   open 0**；e2e=**25 passed**。locks 含 6 个取证器脚本（f1-forensics*.mjs
   ——证据防篡改口径，改动须 unlock）。
4. git 环境备注：提交时 `geometric-repack` 报 Permission denied 为后台
   维护任务权限问题（提交本体成功）；若困扰可 `git config gc.auto 0`。

## 2. 用户裁决台账（2026-08-29 AskUser 四问+附加令——原文尽量保留）

| # | 事项 | 裁决 | 执行落点 |
| --- | --- | --- | --- |
| 决1 | 脉络边框编码 | **A 线型×色阶**：类型=实线(文献)/虚线(主题)；重要度=深青蓝 #2c5f8a 1.5px(核心)/浅灰蓝 #b8c4d4 1px(普通)；白卡无填充；选中=边框再加粗 | U2a |
| 决2 | 重要度数据源 | **D1 修正版**：「同时考虑**被引用数**，且仅限于**开宗立派的研究性论文**」——核心档判定=研究性论文（排除综述/主题）被引边数≥2（被引为主、引用不计入或低权——票面定精确公式）；综述**不得**入核心档 | U2a |
| 决3 | 综述特殊化（附加令） | 「**综述应该排列于脉络最右侧**，用**很淡的灰色虚线**连接涉及的重要的文章」 | U2a（识别+布局+边渲染）+U2b（多参考边数据面） |
| 决4 | 课题切换器 | **上移顶栏**：顶栏=应用名+课题切换器一体（ZCode 式身份区）；侧栏品牌行删除 | U3 |
| 决5 | 衬线字体 token | **保留定义、消费位清零**：--font-display 定义留 theme.css 备将来；年份/标题/品牌/页码全回 Segoe UI/微软雅黑；tabular-nums 保留（功能性） | U3 |

**复测确认项（用户未答，随 F1/F-09 复测一并反馈）**：①灰色选中浓淡
（现 rgba(0,0,0,0.30)≈#B3B3B3，可单值调）；②Esc 后灰选区残留至点击别处
（INV-37 原生语义——不接受则开行为变更小票）；③已高亮区上划选的暗黄绿
观感；④若遇界面冻结立即反馈（取证环境三例暂判环境干扰，需真机背书）。

## 3. U2=F2 脉络修正役（拆两单元——U2b 数据模型冲突前置已裁思路）

### U2a 重制本体（零 schema，先行）

- **已裁决直接执行**（无需再询）：浅色严谨板（白卡+细边框+去渐变/角饰/
  外光；圆角 8px）；夜幕系从脉络域摘除消费（token 留 theme.css 备暗色
  主题）；题名换行=SVG `foreignObject` 内 HTML div（零依赖）+卡高自适应
  （≤3 行+省略+tooltip 全文）——LineageNodeCard:78 单行 text 缺陷根修；
  auto-fit/分档宽(INV-36 nodeWidth 单源)/层带结构保留；年份回标准 UI 字体
  （决5 连带——脉络衬线年份同摘）。
- **决1 边框编码 A**：规格见 §2 表；核心/普通色值对比度已预检。主题节点
  虚线 6-4 沿用现语义（零学习成本）。
- **决2 重要度 D1'**：渲染层纯函数 `isCore(node, edges)`——综述/主题排除、
  被引边（入度）≥2 判核心；公式细节（纯入度 vs 入度≥出度）票面定+单测锁。
- **决3 综述识别 v1**：客户端纯函数 `isSurvey(title)` 关键词启发
  （综述|survey|review|概述|评述，大小写不敏感）——零 schema；**误判人工
  修正通道=后续 D2 式字段增强票**（不在本单）。综述节点布局=整树右缘新列
  （lineage-layout 纯函数改）；其**既有树边**渲染为很淡灰虚线
  （LineageEdges 区分边类型着色）。
- **受锁必然红**：lineage-canvas.test 渐变/角饰/夜色断言+lineage.spec 视觉
  断言——[locked-change] 改写（第三次先例在档）；layout 分档夹具可能连带。
- 票面注意：卡高可变 → NODE_H 常量消费面（layout place/角饰/auto-fit 包围
  盒）连锁；INV-36 宽度单源不动，**高度单源**新增登记候选。

### U2b 综述多参考边（数据模型面——树单父约束冲突处置）

- **冲突**：决3 的「连接涉及的多篇文章」=综述多条参考边，撞 INV-27 树
  单父约束（service 层导入校验+upsertEdge 运行时双口）。
- **预裁倾向（票面前可攻）**：lineageEdge 增可选 `kind: 'ref'`（缺省
  'tree'）——ref 边豁免单父/无环校验（仅限 from=综述节点），shared 受锁
  [locked-change]+迁移（无——edge 表加列需迁移 00x）+service 校验分支+
  手工连线入口（侧板/右键）。**若用户愿降级**：v1 仅渲染综述既有单条树边
  为虚线（零 schema），多参考边延后——复测时问一句。
- 视觉：ref 边=很淡灰（如 #c8cdd6）虚线 2-3；tree 边保持现样式随浅色板
  调淡。

### U2 执行序

U2a 票面→三屋→真机复评（像素差分配方）→U2b 票面（含用户确认多参考边
数据面）→三屋。真机复评放行线=换行在框内+边框编码可辨+整图不回退。

## 4. U3=F3 壳层修正（决4/决5 已定，纯执行）

- **重命名 Synapse Remake→Synapse**（站1 既定）：⚠landmine=userData 目录
  名派生（%APPDATA%/Synapse Remake→Synapse）=**数据迁移第二轮**，复用
  R1-WS1 模式（检测旧目录→迁移入新路径，幂等+旧目录备份一次保留）；
  package.json name/productName+main-window 标题+App 品牌位；e2e 断言
  「Synapse Remake」文本全仓 grep 同步（受锁面 [locked-change]）。
- **顶栏（决4）**：App 壳加 header 条（h-9 级再容纳切换器 +2~4px）承载
  应用名+课题切换器（workspace-switcher 迁位——组件本体逻辑零改，容器
  与样式随壳层）；侧栏品牌行删除。**F-05 防线回归面**：33px 级纵向占用
  对 TabBar/滚动收敛的影响必须真机复评（INV-34 场景）。
- **字体（决5）**：--font-display 消费位清零（lib 衬线年份/页码/设置节标/
  品牌位/脉络年份），定义保留；tabular-nums 不动；--gold-night 别名
  （v1 §2C 观察项）随本单一并评估去留。
- 微项顺手池（v1 §5 存续）：A4 库详情栏留白/A6 课题徽记（顶栏有位了）/
  A7 占位文案——票面时定裁，禁顺手超面。

## 5. U4=F4 文献库卡片等尺寸（微票）

grid `auto-fill minmax(340px,1fr)` 单元格等宽但卡高随内容伸缩——修法=
固定卡高（题名两行截断已有 min-height+venue 单行+标签溢出省略）或
min/max 双向钳制+`grid-auto-rows: 1fr` 兜底。纯 CSS 级（受锁面预期零）。
独立提交（可与 U3 顺手但分票）。

## 6. 遗留池（v2 §5 续+本会话新增）

- **工程遗留**：B1 BAND_LEFT 单源（U2a 重制顺手最佳时机）/B4 F-04 flaky
  第 3 次复现立案口径/B5 backdrop-filter 再现计数/B6 workspace 删除设计/
  B7 M1 策展/B8 venueTier 种子表/**B9 零宽 rect 伪影**（f1 取证发现：
  clientRectsBetween 产 w:0 行盒伪影随锚定三元组入库——渲染无害属脏数据，
  触发=下次触碰 annotation-anchor）。
- **复测反馈驱动微项**：A5 阅读器空态引导+§2 复测确认四项。
- **环境事实**（非工单）：视觉通道缺失（§1.1）/CDP 取证三戒/git repack
  权限警告。

## 7. 教训与回流

- L6（候选先选）本会话**走完制度闭环**：U2 候选案册→用户逐项拍板→案册
  转历史档——后续视觉/方向级决策照此办理。
- L7（延迟预算入验收）已落 e2e 实例（F-06 小票 toolbar≤1.5s）；U2a 真机
  复评应对**边框渲染**补同型预算意识（渲染层纯静态，预期无风险点）。
- 新增（环境类，暂记不升格）：多模态通道缺失的替代取证形态=像素差分+
  DOM 转储（f1 系列配方可复用）；取证器与 CDP 输入互扰三态——二次触发
  再考虑升格 methodology。

## 8. 执行顺序与成本

1. **U2a**→2. **U2b**（含用户确认）→3. **U3**（重命名迁移独立成票单独
   审计）→4. **U4** 微票。每单元三屋全流程；首红/变异日志落盘；交互类
   票面反馈延迟预算入验收。
- 本会话成本账本：U1 主控取证+控制面 ≈6.2M tok；三屋=实现者 1.95M+门一
  0.47M+门二 ≈0.55M；F-09 压缩票 ≈1.8M（含 v5/v5b 核查）；合计 ≈11M。
- 档案指针：战役史=`scripts/audits/f1-campaign-close.md`；取证=`f1-forensics
  .report.md`+五取证器；裁决=ADR-0019（含 F-09 修订）+本文 §2。

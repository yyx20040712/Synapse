# AI 辅助开发（Vibe Coding）复盘与经验教训

> **来源**：AquaResearch → Synapse → Synapse_remake 三个项目的完整经历
> **日期**：2026-08-21
> **用途**：下次 AI 辅助开发项目开工前必读；也是《生产级软件工程》课程 L37-L44（规约/审查/AGENTS.md/代理编排/TDD+AI）的实战对照
> **原则**：本文档的所有结论都有项目中的具体证据（文件路径 + 数字），不是泛泛而谈

---

## 一、项目时间线（事实）

| 项目 | 定位 | 规模 | 架构 | 结局 |
| ---- | ---- | ---- | ---- | ---- |
| **AquaResearch** | 第一次尝试 | 241 文件 / 43,275 行 / 51 commits（8 天） | 三进程：React + Rust(Tauri) + Python sidecar | 系统性腐烂，已归档 |
| **Synapse** | 复盘后重写 | 58 文件 / 4,455 行 | 两层：React + Rust，砍掉 Python | 方向正确但未收尾 |
| **Synapse_remake** | 第三次尝试的前兆 | 仅测试残留文件 | — | 已叫停（本文档所在） |

**核心事实**：同一产品（学术 PDF 阅读器 + 文献管理器）经历了至少三次开发尝试。第一次 8 天产出 43,275 行后"死亡螺旋"；第二次刻意缩小到 1/10 规模；第三次在开始前被叫停。

**最重要的洞察：三次尝试不是三次独立失败，而是一次失败的三次重演。** 每次重写都重置了已修复的 bug，却没有让根因消失。根因不在"代码太烂"，而在流程——三次尝试都以"无 git 基线 + 无 CI + 无测试基线 + 无规约"的方式开始，重写没有改变任何一条流程变量。

---

## 二、三个核心症状与根因

### 症状 1：AI 幻觉导致修 bug 困难

**根因：正确行为不可验证。** 修 bug 时 AI 不知道"对"长什么样，只能猜；猜错就表现为幻觉。

证据（来自项目）：

- 3 个 Rust Agent 并行写代码 → **125 个编译错误**；`PaperFilter` 被两个 Agent 各定义一次（Synapse/docs/RETROSPECTIVE.md）
- 8 个 Rust→Python 方法名不匹配，直到运行时才暴露
- **最典型的一处**：AquaResearch CHANGELOG v0.2.0 承认 "All `deny_unknown_fields` removed (23 instances) from IPC structs"——为了绕过跨层字段漂移，**删掉了类型保护本身**。这是"让 bug 闭嘴"而非"修 bug"，是无可验证合约时 AI 唯一能做的事
- 修 bug 的经验档案 `docs/PROCESS.md`（65KB）是双重编码损坏、基本不可读——最该被 AI 参考的历史，AI 读不了

### 症状 2：代码淹没在上下文中

**根因：超大文件 + 重复资产 + 矛盾文档。** 上下文窗口放不下，AI 每次只见局部、只见一个版本。

证据：

- `src-tauri/src/db/sqlite/repos.rs` **3,303 行**；`python-engine/src/methods.py` 1,261 行；`download/manager.py` 1,077 行——AI 读一个文件就吃掉半个上下文窗口
- **pdf.js 资产 5 处重复副本、共约 7.1MB**，包括 `dummy-non-existing-folder/pdfjs/`（目录名本身就是混乱的产物）
- **多套 PDF 方案并存**：README 声称 "No dependency on PDFium"，但 4.6MB 的 `public/pdfium.wasm` 就在那里；FEATURES.md、AI_GUIDE.md 各说各的
- 孤儿代码：`FolderTree.tsx`（463 行）、`TagInput.tsx`（327 行）全库无人引用
- 文档数字互相矛盾：测试数在 README(30)、ARCHITECTURE.md(75)、其他文档(52/59) 之间漂移；AquaResearch README 声称 452 测试，实际清点约 90(Rust)+174(前端)+99(Python)

### 症状 3：越做越乱

**根因：没有任何防线在真正运行。** 每次修改都在无保护状态下进行，错误只能累积不能拦截。

证据：

- **最讽刺的**：Synapse 写好了 `.github/workflows/ci.yml`、`scripts/pre-commit.ps1`、全套文档——但**没有 git 仓库，CI 根本不可能运行**。防线建好了，没通电
- **测试在撒谎**：`e2e/smoke.spec.ts` 的断言文本是乱码（`娌℃湁鎵撳紑鐨勬枃妗?`）——它永远匹配不上真实 UI，这个测试**永远"通过"**，提供虚假的安全感
- 占位实现伪装成功能："Phase 8 — not implemented" 存根、`StubTranslationEngine`、`Placeholder` 注释遍布代码
- 文档与代码脱节：Synapse `vite.config.ts` 注释说 "iframe isolation"，而复盘文档已判定 iframe 方案被 WebView2 阻止、早已放弃

---

## 二·五、有效修正：Synapse 做对的事（与失败对照）

复盘不只是记录失败——**Synapse 对 aquaresearch 死因的架构修正，是这次经历最有价值的资产**，必须提炼为可复用规则：

| # | aquaresearch（失败） | Synapse（有效修正） | 规则 |
| - | -------------------- | ------------------- | ---- |
| E1 | 三进程（React + Rust + Python sidecar），跨层契约靠 18 个 JSON Schema 手同步 | 砍掉 Python 层，两进程 | **进程数每多一层，契约维护成本翻倍。** 能用两层解决的，不要引入第三层 |
| E2 | quicktype 生成 TS/Rust/Python 三套类型，手动保持同步 | `#[derive(TS)]` + ts-rs 编译时生成 | **类型只从单一源头生成，任何"手动保持 N 处一致"的约定都会在 N≥2 时失守** |
| E3 | Diesel ORM + 12 轮 migration | rusqlite 直连 + 1 个 migration | 为小数据量的桌面应用引入重型 ORM 是过度设计；**复杂度按需引入，不按"工程感"引入** |
| E4 | `repos.rs` 3,303 行 / 多实体混杂 | 最大文件 429 行 | **文件 ≤500 行是硬规则**——它决定了 AI 上下文能否完整覆盖一个单元 |
| E5 | 多套 PDF 方案并存（PDFium / pdf.js / 手动 span） | 单一方案（canvas + TextLayer） | **方案切换后删除旧方案**——并存是给 AI 制造"权威冲突" |

这些修正证明：**第二次尝试的方法论是对的，缺的是把方法论变成机制（git + CI + 测试），以及收尾纪律。**

---

### A. 关于 AI 协作者：先定义"对"，再让 AI 找路径

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| A1 | AI 生成代码与既有代码不兼容（类型、命名、API 各写各的） | 规约只给了行为层，没给接口层/架构层 | **给代理写五层规约**（行为/接口/架构/生命周期/文化）——参考课程 L37。指定复用哪个函数、粘贴关键 model 字段、否定式声明"不用 X 用 Y" |
| A2 | 跨层类型漂移反复出现，最后靠删保护绕过 | 三层（TS/Rust/Python）手动同步类型 | **类型同步用生成器，不用手写**：ts-rs 编译时生成（Synapse 已做对）、或 JSON Schema + quicktype；任何"手动保持两处一致"的约定都注定失守 |
| A3 | 多个代理并行产出互相矛盾 | 并行写共享状态/同文件 | **并行派发规则：写不同文件 = 可并行，写同文件/共享状态 = 串行**（课程 L40）。Synapse 的 125 编译错误就是同文件并发写的代价 |
| A4 | AI 占位实现混入真功能 | 没有"完成定义" | 规约里写**验收标准**：端点有测试、`grep "not implemented"` 返回 0。占位就是未完成，未完成不能合入 |

### B. 关于代码组织：文件是上下文的基本单位

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| B1 | 单文件 3,303 行，AI 读不完、改不动 | 增长无上限 | **硬规则：文件 ≤500 行**，超出即拆。Synapse 最大文件 429 行——证明拆得动 |
| B2 | 死代码堆积（孤儿组件、重复资产、stub） | 没有回收机制 | **死代码即删**：提交前 `grep` 未引用文件；`dummy-non-existing-folder`、重复 pdf.js 这类资产就是上下文的噪音 |
| B3 | 多套方案并存（PDFium / pdf.js / 手动 span） | 切换方案时没清理旧的 | **方案切换 = 删除旧方案**，不是并存。并存会让 AI 无法判断"当前真相" |

### C. 关于文档：文档不能代替验证

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| C1 | 写了 230KB docs 仍越做越乱 | 文档是"建议"，不是"执行" | **文档 + 自动化执行才是纪律**。规则要写进 CI/lint 钩子，而不是只写进 .md（LESSONS.md L13 的原话："Architecture docs without automated enforcement are suggestions"） |
| C2 | README 与现实脱节（声称无 PDFium 但文件在） | 文档没有同步机制 | **矛盾时删文档，不是留两份**。留两份 = 给 AI 的"权威冲突"，AI 会随机选一个信 |
| C3 | 文档数字互相矛盾（测试数 30/52/59/75/452） | 手写数字必然过期 | **文档不写具体数字，写"以 CI 为准"**。机器生成的数字（测试数、覆盖率的入口）是 CI，不是 README |
| C4 | 关键档案编码损坏（PROCESS.md 双重编码） | 工具链编码不一致 | 中文内容统一 UTF-8；AI 写中文文档后必须**用工具验证可读**（乱码断言就是没验证的代价） |

### D. 关于测试：测试是合约，不是摆设

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| D1 | 测试断言是乱码，永远通过 | 没验证测试本身有效 | **每个测试必须能失败一次再通过**。乱码断言 = 假阳性 = 虚假安全感，比没有测试更危险 |
| D2 | 改动后旧功能悄悄坏了 | 没有行为保护 | **核心模块写 Characterization Test 锁定当前行为**（课程 L35），之后 AI 改代码有围栏 |
| D3 | 测试数字被反复改写、不可信 | 没有单一真相 | 测试入口统一：`npm test` / `cargo test` 一键跑全量；CI 是唯一权威 |

### E. 关于版本控制：git 是 AI 协作的地基

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| E1 | Synapse 无 git 仓库，CI 无法运行 | 建了防线没通电 | **git init 是第一天的事，不是最后一天**。没有版本控制，任何修复都是赌博——AI 的错误无法回滚 |
| E2 | 修改不可追溯 | 无提交纪律 | 每次 AI 改动一个 commit；`git diff` 审查后再合入（课程 L38 审查纪律） |

### F. 关于重写：重写是最贵的修 bug 方式

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| F1 | 同一产品第三次重写已开始 | 前两次失败 → 归因于"代码太烂" → 重写 | **重写重置所有已修复的 bug**（课程 L35）。先问"根因是否在流程而不在代码"——三个项目的问题是流程问题（无测试/无 CI/无规约），重写不解决流程 |
| F2 | 重写后旧问题复发 | 方法没变 | 重写前先建立：git + CI + 测试基线 + AGENTS.md。没有防线的重写是换一个坑再跳 |

### G. 关于上下文管理：让 AI 每次只见"该看的"

| # | 现象 | 根因 | 规则 |
| - | ---- | ---- | ---- |
| G1 | 代码淹没在上下文 | 项目太大、噪音太多 | **小文件 + 死代码即删 + 单一方案** = 上下文干净；用 @explorer 先探索再派 @fixer（课程 L40），不让实现者探索 |
| G2 | 上下文缺关键信息（文件路径、版本、现有 API） | 规约不完整 | 规约中**写全文件路径 + 粘贴关键代码 + 声明版本**（源头路由/自包含/权威三纪律，课程 L37/L42） |

---

## 四·五、可执行模板（直接复制使用）

### 五层规约骨架（每层缺一不可——课程 L37）

```markdown
## 任务：[一句话]

### 行为层（做什么）
- [ ] 功能点 1
- [ ] 功能点 2

### 接口层（输入/输出形状）
- [ ] API：METHOD /path  {req} → {resp}
- [ ] 数据模型字段：[列出]

### 架构层（如何融入现有系统）
- [ ] 复用现有模块：[文件路径 + 函数名]
- [ ] 新文件位置：[路径]
- [ ] 数据库变更：[新表/新列/新索引]

### 生命周期层（未来如何演变）
- [ ] 预留扩展点：[列出]
- [ ] 明确不做的事：[列出，防止 AI 顺手实现]

### 文化层（团队约定）
- [ ] 错误处理：[Result / AppError]
- [ ] 日志：[structlog]
- [ ] 测试：每个端点 ≥1 个 pytest；`grep "not implemented"` 返回 0
```

### AGENTS.md 规则条文示例（具体 + 否定式禁令——课程 L39）

```markdown
## 代码
- 文件不超过 500 行；超出必须拆分为独立模块并各自命名
- 禁止 `except Exception` 和裸 `except:`；可预期错误返回 Result[T, AppError]
- 禁止 `#[allow(dead_code)]` 不带注释说明；未使用代码必须删除
- 重复出现 3 次以上的逻辑必须抽函数（第二次保持，第三次抽象）
- 所有路径、数据库操作使用参数化查询；禁止字符串拼接 SQL

## 修改
- 每次改动一个逻辑单元，完成后 `git diff --stat` 自查范围
- 改动核心模块（PDF 渲染、数据层）前，先跑该模块的 Characterization Test
- 新功能必须自带测试；测试必须能"先失败一次再通过"

## 提交
- 中文注释与文案统一 UTF-8；提交前验证文档可读（无乱码）
- 不提交占位实现（"not implemented"/"placeholder"）
```

### 完成定义（Definition of Done）清单

- [ ] 功能代码 + 测试全部提交
- [ ] 全量测试通过（`npm test` / `cargo test`，非 README 数字）
- [ ] `grep -rn "not implemented\|placeholder\|TODO\|FIXME" src/` 返回 0
- [ ] `git diff --stat` 无范围蔓延（只改任务相关文件）
- [ ] 无死代码新增（新文件被引用；无重复资产）
- [ ] 中文内容无乱码（用工具/编辑器验证可读）

### 提交前预防性检查命令（把规则变成可执行检查）

```bash
# 1. 占位实现与未完成标记
grep -rn "not implemented\|placeholder\|TODO\|FIXME" --include="*.ts" --include="*.rs" --include="*.py" src/ || echo "OK"

# 2. 裸异常（静默吞错）
grep -rn "except Exception\|except:" --include="*.py" src/ || echo "OK"

# 3. 死代码（未被引用的新文件）
git diff --stat        # 人工核对：新增文件是否被 import

# 4. 范围蔓延
git diff --name-only   # 只应包含任务相关文件

# 5. 乱码（mojibake）——GBK 解析 UTF-8 的典型特征
grep -rn "锟斤拷\|娌℃湁\|鎵撳紑\|鏄痑" --include="*.ts" --include="*.rs" --include="*.py" --include="*.md" . || echo "OK"
```

---

## 五、行动规则速查表（下次项目开工前逐条打勾）

- [ ] `git init` 完成，首次 commit 打基线
- [ ] CI 配置完成且**在真实 git 仓库上跑过一次**
- [ ] `npm test` / `cargo test` 一键跑通全量测试
- [ ] 测试基线确定（不是 README 里的数字，是 CI 输出）
- [ ] AGENTS.md 已写：具体规则 + 否定式禁令（不是"写干净代码"，是"禁止裸 except；文件 ≤500 行；死代码即删"）
- [ ] 规约模板就绪（五层：行为/接口/架构/生命周期/文化）
- [ ] 类型同步用生成器，无手动双写
- [ ] 目录清理：无重复资产、无 `dummy-*`、无孤儿组件
- [ ] 中文内容统一 UTF-8，AI 产出经工具验证可读

## 六、给未来自己的三条守则

1. **先有防线，再写代码。** git、CI、测试基线、AGENTS.md 四件事在写第一行业务代码之前完成。防线的价值不在"好看"，在于让 AI 的错误在合入前现形。

2. **AI 是放大器，不是替代品。** 好的工程判断 × AI = 10 倍效率；差的工程判断 × AI = 10 倍技术债。判断力来自：可验证的验收标准、可回滚的版本控制、可信任的测试。

3. **重写之前，先修流程。** 如果第三次尝试还是"无 git + 无 CI + 无测试"地开始，结果已经写在本文档里了。

---

## 七、下一步行动（把教训变成机制）

本文档的价值只有在落地时才存在。以下是明确的行动清单（按优先级）：

| 优先级 | 动作 | 对应教训 |
| ------ | ---- | -------- |
| **P0** | 给 Synapse 补 git 基线：当前状态做首次 commit | E1（无版本控制，AI 错误无法回滚） |
| **P0** | 修复 `smoke.spec.ts` 的乱码断言（`娌℃湁...` → 正确中文） | D1（假阳性测试 = 虚假安全感） |
| **P0** | 删除 `dummy-non-existing-folder/` 与重复 pdf.js（保留一份） | B2（重复资产 = 上下文噪音） |
| **P1** | 推 GitHub 并跑通 CI（补 `package.json` test 脚本、验证 pre-commit 钩子） | C1（文档无自动化执行 = 建议） |
| **P1** | 删掉各文档中的测试数字，统一为"以 CI 为准" | C3（手写数字必然过期） |
| **P2** | 对 PDF 渲染核心写 Characterization Test | D2（给 AI 一个"不能破坏"的围栏） |
| **P2** | 写 AGENTS.md（用上文模板，从 LESSONS.md 提炼） | A1/A4（给 AI 的宪法） |

**验收标准：完成 P0 三项后，Synapse 才有资格继续开发；完成 P1 后，AI 的每次改动都会被机制验证，而不是靠信任。**

---

> **本文件的位置本身就是一个提醒**：Synapse_remake 目录里只有测试残留文件（L6_verify.db、test.txt、hello.txt）——第三次重写差点在没有任何防线的情况下开始。现在它成了经验教训的存档处。**下次开工，让 Synapse 收尾，而不是再开一个新目录。**

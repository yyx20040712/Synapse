# F1 划选修正役·收官报告（SR2-F-08——2026-08-29 本会话）

> 战役：LOOP v2 四修正役之首（复测站 3「划选全面失败+0.5~5s」）。
> 路线：ADR-0019 R1（回退官方原生半透明+删自绘层）。
> 三屋：实现者 DONE → 门一 PASS（0B/4W/7N）→ 门二 PASS（零回炉）→ 主控收口。

## 1. 交付

- 划选视觉=**原生 ::selection rgba(0,0,255,.25)**（与官方 pdf.js/Firefox 同款）：
  拖选第一帧即反馈（零 JS 链路，INV-37）；`SelectionRects.tsx` 删除（P10）；
  SelectionLayer 收窄为「选区→工具条→保存」。
- 受锁两文件第三次改写 [locked-change]：e2e F-06 小票（官方半透明精确值断言
  +selection-rects 0 计数防回归+**L7 延迟预算 toolbar≤1.5s**——L7 制度化首个
  实例）；unit F-07a→F-08 防回归守卫（变异红证在档）。
- INV-37 登记（含 Escape 后 tint 残留语义=视觉-状态严格同步）；ADR-0019；
  取证全档（f1-forensics.report.md+四取证器+JSON）。

## 2. 收口证据链（全绿）

| 关 | 结果 |
| --- | --- |
| verify 七环 | exit=0：open 0 / locks 159 / 104 文件 **858 用例**（859−1=F-07b 删，与预测一致）/lint/typecheck/build 全绿 |
| e2e 全量 | **25 passed（1.2m）exit=0**——重写守卫实弹通过（rgba 序列化=`rgba(0, 0, 255, 0.25)` 与正则吻合） |
| 真机复评 v4 | **部分达标**：::selection computed=官方值（实机日志）；原生蓝 tint 在场（差分 blueTint=63 采样点/词选态 vs 基线）；两项转用户复测（见 §4） |

## 3. 成本账本（本单元）

| 屋 | token | 时长 |
| --- | --- | --- |
| 主控（取证 4.2M + 控制面/收口） | ≈4.2M+2.0M | — |
| 实现者 | 1,951,408 | 577s |
| 门一 | 474,527 | 339s |
| 门二 | ≈5.5×10^5 | ≈330s |

## 4. 用户复测清单（站 3 复评口径）

1. 拖选应**即时**见蓝色半透明选区（浏览器原生，无延迟）；松手后工具条 ≤1.5s。
2. **观察点 A**（INV-37 声明语义）：按 Esc 工具条收起，但蓝色选区**保留至点击
   别处**——若不接受此交互，开新行为变更单（非缺陷）。
3. **观察点 B**：已高亮（黄）区域上再划选——原生蓝×multiply 黄呈暗绿，观感
   可接受性请反馈。
4. ⚠ 若真机使用中出现**界面冻结**请立即反馈：取证器环境曾三次出现渲染冻结
   （见 §5-3），e2e 25 项真流程零冻结、暂判环境干扰（僵尸 playwright 驱动/
   CDP 输入会话干扰），无应用侧冻结证据，但需真机使用背书。

## 5. 遗留与教训

1. **B9 零宽 rect 伪影**（取证发现）：clientRectsBetween 产 w:0 行盒伪影随锚定
   三元组入库（AnnotationLayer 渲染无害但属脏数据）——遗留池，触发=下次触碰
   annotation-anchor。
2. **主控视觉通道缺失**：本环境 Read/子代理 Read 图像均只回 CDN URL、
   analyze_image 对 CDN 签名 URL 400——多模态评审全程以「无阈值像素差分+
   DOM 状态转储」替代（对本役反而更客观）；后续视觉役须知此环境事实。
3. **取证器环境干扰三态**（v4 三跑三挂点：拖拽中截图打断 CDP 输入→mouseup
   丢失；toolbar 假超时；渲染冻结 evaluate 无界挂）——已修 r4/r5 部分防挂，
   但冻结根因未追（e2e 背书+loop 防线止损）；僵尸 playwright 驱动进程未清
   （禁 blanket kill node——有自伤风险）。
4. 教训回流：L7 已随本役落地首个 e2e 实例（预算断言形态=timeout 收紧）；
   L6 的候选先选制度已用于 U2（见候选案册）。

## 6. 下一步（本会话余量已尽，交接）

- **U2=F2 脉络**：候选案册已备 `docs/design/2026-08-29_lineage-reroute-options.md`
  （边框编码 A/B/C 三案+重要度数据源 D1/D2 两案，**待用户选**后票面化三屋）。
- U3=F3 壳层（重命名含 userData 迁移 landmine）/U4=F4 卡片等尺寸：未动，
  按 LOOP v2 §3 顺序推进；U3 有两处用户选择点（切换器位置/字体处置粒度）。

== reasoning ==
We need audit diff. Need output JSON only. Need thoroughly inspect.

We have project constitution, work order brief, diff. Need evaluate contract/discipline, correctness, security, prod quality, test discipline.

Diff includes docs/invariants, locks manifest, bootstrap, ipc/export, corpus.assemble, corpus.export.service (big), export.service guard, interface-template new, services/index, CorpusExtractor comment, app-error, api-surface, schemas, test new, registry.

Need identify findings. Let's inspect carefully.

Constitution hard rules:

- State machine prerequisite for store+async+user input. Work order says behavior spec state machine? Maybe brief says "会话状态机照票面（idle/preparing/streaming/finalizing→done/failed+单飞 EXPORT_BUSY；SessionError 结构化 code 经 toAppError 保留）". Implementation has session state semantics via session variable (null active, current paper etc). Need check state transitions, races.

- File ≤500 lines. corpus.export.service.ts diff shows file likely 285 lines? Need check after addition maybe ~380? We don't know but maybe fine. New test file 374 lines + header maybe under 500? The diff says new file 374 lines, okay under 500 maybe plus header 374? likely.

- Layer: ipc→services→repos→db renderer→window.api→ipc. In ipc/export_.ts calls deps.services.export_.exportCorpusSession after dialog. OK. Service imports repos etc. Renderer CorpusExtractor no Node API. OK.

- Security: no SQL injection? seed test uses db.prepare placeholders. Service uses repos. No shell, no eval. openExternal no. URL app-file:// custom. File system path handling: exportCorpusSession receives dir from pickFolder in main. OK INV-07. But is dir validated as absolute? pickFolder returns. Tests use tmpdir. Could be path traversal? paperIds from renderer, used to construct file paths: `${id}.md` etc. If paperId contains `../` or path separators? This is critical. Paper IDs from DB listAllIds or renderer req. In service, paperIds are validated schema strings min1 max? corpusSessionReqSchema only z.array(z.string().min(1)).min(1).max(1000), not disallow path separators. queue uses paper.id from DB detailById(id), not raw rendered. In preparing, for each id, detailById; if detail exists, writeFile join(dir,'corpus', `${id}.md`) uses id (DB key possibly arbitrary). If attacker can control paperIds to existing ID? IDs are DB primary keys maybe uuid? Need know schema. Probably paper id generated maybe uuid. But if user can pass arbitrary paperIds that match existing rows? Renderer API can call with any paperIds; service only gets IDs from input. If id "p-1" OK. If malicious renderer (or XSS?) can pass id with `../../outside`, detailById would need DB row; if such id in DB, could write outside. But ID from DB could be user-created? Papers inserted via import, id maybe uuid generated by main. Not directly user-controlled? Need examine. However path traversal through `paperId` from IPC is a potential security issue: `${paperId}.md` in join. If paperId contains `../`, join normalizes and can escape. But to get detail not null, DB must have row with that id. If DB primary key can be controlled by renderer? Probably papers.repo generateId. Could maybe not. But corpusItem req.paperId must match current.paperId from DB. annotations figure names: annotationId appended to `anno-${req.annotationId ?? 'unknown'}.png`; annotationId from DB. page from req.page number? schema type maybe number. payload base64. No direct traversal. `req.figure === 'anno' ? 'anno-...' : page`. If annotationId contains ../? DB id likely uuid. Not high.

Need inspect schemas diff: corpusSessionReqSchema paperIds max 1000 but no description. corpusSessionResSchema. It says "目录经 main 侧系统对话框（INV-07）service 内选" but code ipc layer picks folder. Comment in schemas says service 内选? Actually "目录经 main 侧系统对话框（INV-07）service 内选" maybe inconsistent: service receives dir, not "service 内选". Minor.

Need check actual logic for races/state.

Implementation key:

- `createCorpusExportService(deps)` returns object. Holds `let session`. `exportCorpusSession(input)`:

```
if (session !== null) throw EXPORT_BUSY
const ids = input.paperIds ?? repos.papers.listAllIds()
let created: ActiveSession | null = null
const promise = new Promise((resolve, reject) => {
 created = {...}
})
const active = created as unknown as ActiveSession
session = active
try {
 await cleanRebuild(input.dir)
 for (const id of ids) {
   detail = repos.papers.detailById(id)
   if null errors/done continue
   managedPath = fileStore.resolveManagedPath(detail.fileName)
   fileExists true
   await readFile(managedPath).catch(() => false)
   if !fileExists errors/done continue
   md = assembleCorpusMd(...)
   await writeFile(...)
   active.queue.push(detail)
 }
 await advance(active)
} catch(e) { await failSession(active, ...) }
return promise
```

Potential issue: If cleanRebuild fails, catch failSession? cleanRebuild is inside try, so if fails, failSession calls `session=null`, rejects. But `active` was set. However if `cleanRebuild` fails before creating dirs, fine. But failSession uses `s.dir`, rm tmp. OK.

But crucial: `session = active` before `cleanRebuild`. If two concurrent exportCorpusSession calls: first sets session non-null synchronously before await? Yes, function executes synchronously until first await: it sets session then calls `cleanRebuild` (async) and returns promise. Second call sees session non-null. Good.

However `corpusItem` called while exportCorpusSession still in preparing phase (before advance) with current null: throws INVALID_REQUEST. OK.

Race: If `advance` encounters queue empty? But we push all prepared details before advance; if no papers, advance finalizes manifest with empty papers. Resolve fileCount 0. OK.

Potential error: `advance(s)` is async and called without awaiting in exportCorpusSession (`await advance(active)` yes). In finishPaper, `await advance(s)`.

One issue: `startPaper` doesn't await sendProgress; fine.

`finishPaper`: 
```
const cur = s.current
s.current = null
s.done += 1
if !outcome.ok errors.push, await advance
...
const fulltext = cur?.fulltext.join('\f') ?? ''
writeFile fulltext...
fulltextSha from fulltext string. contentSha read file. push manifest. await advance
```

Potential fulltext is empty if no fulltext chunks. It writes empty file. OK.

Manifest `file: corpus/${paperId}.md` but maybe should be relative to dir; correct.

Error handling: `corpusItem` catch:

```
try {
 ...
} catch (e) {
 if (e instanceof SessionError && e.code === 'INVALID_REQUEST') throw e
 await failSession(s, ...)
 throw e
}
```

But if `finishPaper` throws IO error after it has completed `s.done+=1`, `s.current=null`, and maybe before `advance` pushes next? In finishPaper, if writeFile fulltext fails, it throws; catch calls failSession, session null, reject. But `s.done` increment happened; no matter. If `outcome.ok` false branch pushes error then calls advance; if advance later throws? Actually finishPaper call sites in corpusItem: if req.kind error, calls `await finishPaper(... {ok:false})`. If that throws, catch failSession. But finishPaper may have already advanced and started next? Wait finishPaper with ok false pushes error then `await advance(s)`. advance if next exists calls `await startPaper(s,next)` sends event and returns. No IO except maybe no. If no next finalizing writes manifest and resolves. If write failure, catch in corpusItem sees error (not SessionError INVALID_REQUEST), calls failSession. But `finishPaper` might have already called `advance`, which may have already resolved/rejected the session promise? Specifically if finalizing writeFile succeeds and resolves, then finishPaper returns; no catch. If writeFile fails, session hasn't resolved; failSession rejects. Good.

But there is a subtle issue: `finishPaper` for complete detail null throws SessionError('INTERNAL'), catch checks if INVALID_REQUEST? no, then failSession, then rethrow. But the service's internal error classified as INTERNAL, failSession rejects session with IO_ERROR maybe not ideal. Also if `detailById` returns null, data inconsistency. Minor.

More important: The session promise is created but never rejected on `cleanRebuild` failure? failSession rejects. OK.

But what if `exportCorpusSession` throws synchronously before promise created? EXPORT_BUSY. OK.

Potential `session` global and multiple service instances? Factory creates one. Good.

Potential "current" with fulltext buffer: `cur.fulltext.push(req.payload)`; payload may be large, but per page.

Potential `sendExportEvent` default no-op in services/index. In bootstrap injects. But `services/index.ts` imports BrowserWindow? No, bootstrap has. OK.

Potential channel name conflict: api-surface has `corpus: { channel: 'export/corpus' }` (C-02 old) and new `corpusSession: { channel: 'export/corpus-session' }`. The service comment says "通道名=export/corpus-session（母本 §2.3 的 export/corpus 已被 C-02 单篇导出占用——更名避撞）". OK.

IPC export_.ts: `corpusSession` handler uses `deps.dialogs.pickFolder()`. Need check `ExportIpcError('CANCELLED', ...)` exists? It throws. Maybe C-02 exportTo uses same. OK. But concern: single-flight should also include dialog time? Comment says "dialog 期间并发由模态性+BUSY 兜底". Single-flight judged in service after dialog. User can open two dialogs? pickFolder modal? OS maybe modal to window but multiple windows? Single window. Not concern. But if user cancels dialog, no EXPORT_BUSY; no session. OK.

Potential contract: `corpusSession` IPC handler resolves `CorpusSessionRes` with dir from dialog, but service returns same. OK.

Potential "fileCount" should count only successful papers (s.papers.length). Tests expect. OK.

Potential "errors[]" optional. In manifest they include if >0. Good.

Potential `cleanRebuild`: rm manifest/tmp; rm sub dirs; recreate; write INTERFACE.md. It doesn't remove `figures`? yes. It doesn't remove other files. Good.

Potential atomicity: "manifest 终局单写（临时文件+rename 原子替换）"; if rename on Windows fails when target exists? They removed old manifest at start, so target doesn't exist. If another process writes? Not concern. However `rename(temp, manifest)` on Windows atomic replace? Node's rename can replace existing file on Windows? Yes MoveFileEx with replace? likely. But they removed old at start, so no target. Good.

Potential "存在=就绪" no window? They write tmp then rename; if crash between write and rename, old manifest removed at session start, tmp remains, no manifest => not ready. But tmp remains, next session clean removes. Good.

Potential "failSession" does `rm(tmp)` then reject. If failSession itself fails? catch undefined. OK.

Potential "SessionError" not mapped to AppError? Need check register toAppError handles. AppError codes includes EXPORT_BUSY. But SessionError is a class. toAppError likely recognizes Error with code? Need check existing app-error maybe. The service throws SessionError from exportCorpusSession; IPC layer may catch and convert via toAppError? Need inspect? Not in diff. Work order says "SessionError 结构化 code 经 toAppError 保留". Likely existing. No issue.

Potential issue: `questionOrder(q: string)` returns `Number(q.replace('Q',''))`; if q not "Q#" or "divergence"? AiNoteQuestion type maybe union of Q1..Q7/divergence. OK. But if `Number('')`=0 for any invalid; type safe.

`ROLE_ORDER: Record<AiNoteRole, number>` with values; if role undefined? type.

`orderAiNotes` maps content `${n.question}: ${n.contentMd}`. INTERFACE says `[ai:<model>] <question>: <回答>`. OK.

Potential "R12 单源": assembleCorpusMd uses orderAiNotes inside service? Wait service calls `orderAiNotes(deps.repos.aiNotes.listByPaper(id))` then passes `aiNotes` to `assembleCorpusMd`. Need see assembleCorpusMd signature and how it consumes aiNotes. In diff, assembleCorpusMd input has `aiNotes: AiNoteEntry[]` union? Actually diff defines `OrderedAiNoteEntry extends AiNoteEntry`, but assembleCorpusMd likely expects AiNoteEntry[]. It probably uses `content`, `source`, `page`. OK.

Potential "corpus md golden = assembleCorpusMd output" test. They call service which writes md. They recompute expected using same pure function. This is golden vs same function; if service and function both wrong same, test wouldn't catch content. But other assertions `[user]` and `[ai:glm-5.3]` plus exact equality ensure service uses pure function. However if pure function expected output wrong, test not validating content. But work order says golden=assembleCorpusMd output; accepted.

Potential test issue: `h.missingFiles.add('p-bad.pdf')` but `fileStore.resolveManagedPath` uses `fileRef` from `detail.fileName`; `seedPaper` inserts file_ref `${id}.pdf`, so fileStore gets `p-bad.pdf`, missingFiles contains `p-bad.pdf`. OK.

Potential "幂等重导" test: It runs second export. But `h.svc.exportCorpusSession({dir:h.dir})` calls cleanRebuild, removing previous manifest, dirs. It then prepare writes corpus; emulate extractor with nextExtract(h, extractSeq). Note `extractSeq` increments from 0 to 1. First run nextExtract(h,0) returns req index 0; after session complete events array contains one extract-request. Second run nextExtract(h,1) waits for second. OK. But first run's `emulateExtractor` uses `await h.svc.corpusItem` for each; events list grows but no extract. Fine.

Potential test "清空重建" writes `manifest.tmp.json` to dir; cleanRebuild removes tmp. Good.

Potential test "落盘失败" creates directory `figures/<paperId>/page-1.png` after nextExtract. But cleanRebuild before preparing has already recreated figures/<paperId>? Actually cleanRebuild removes figures dir and mkdir figures; prepare creates no figures subdir until corpusItem figure. So they mkdir recursive creates figures/p-1/page-1.png as directory. Then call fulltext first, OK. Then figure writeFile to `figures/p-1/page-1.png` where path is directory -> ENOTDIR. Good. But they call `await h.svc.corpusItem({... fulltext ...})` then expect figure rejects. Since fulltext was pushed, current fulltext. OK.

Then `await expect(session).rejects.toThrow()`. The figure corpusItem calls failSession, rejects session with SessionError('IO_ERROR', ...). The corpusItem itself rethrows e (ENOTDIR) so rejection. Then remove figures dir and retry. However note: after failSession, `session=null`. But `current` remains? failSession sets session=null but doesn't alter s.current. New session new object. OK.

Potential issue: `corpusItem` catch calls failSession, then `throw e`. For ENOTDIR e not SessionError so failSession. Good.

Potential issue: If `corpusItem` receives `kind: 'complete'`, finishPaper calls advance; advance may call startPaper next and send event before corpusItem returns. Then corpusItem returns ok true. If no next, finalizing writes manifest and resolves session promise. Good. But if figure write fails, failSession. Good.

Potential race: `corpusItem` for fulltext pushes to `cur.fulltext` but doesn't verify page order or that payload is not already duplicate. extractor likely sends pages in order. Could duplicates cause fulltext duplicated. Not specified.

Potential "INVALID_REQUEST defense" semantics: If no session or current null (e.g., while in preparing/finalizing), throws INVALID_REQUEST. This could happen if renderer sends late response after session moved to next paper? It checks `req.paperId !== s.current.paperId`; if mismatch, reject. In `startPaper`, sends extract-request for next paper only after previous finishPaper. But renderer may respond for previous paper after finishPaper already advanced? Actually finishPaper is called from corpusItem when receiving complete. Then advance sends next request. Renderer won't have previous paper after complete. But if out-of-order (e.g., fulltext after complete) could happen? IPC messages order preserved? Maybe. Defense throws INVALID_REQUEST, caller (renderer CorpusExtractor) maybe treats as error? But it doesn't disturb session? If current is next paper, a late fulltext for previous is rejected, next continues. OK.

Potential issue: The session's `queue` contains PaperDetail objects. `startPaper` uses `deps.repos.annotations.listByPaper(paper.id)` each time. If annotations change during session? unlikely.

Potential issue: `fileCount: s.papers.length`; "fileCount" maybe should count all files? C-02 export returns fileCount of md files? For five-piece maybe should be paper count. OK.

Potential issue: If no successful papers but errors, manifest has `papers: []`, errors. Should "manifest exists=ready" with fileCount 0 and errors? Maybe tool can activate but no corpus. Should be OK? Maybe not; but session "done" with errors. User sees error. Not blocking.

Potential issue: `cleanRebuild` writes INTERFACE.md before preparing; if later fails, no manifest but INTERFACE.md remains. INV-18 "中断=无 manifest=工具侧不可激活" yes. Good.

Potential issue: The IPC layer `corpusSession` handler uses `req.paperIds` but `deps.dialogs.pickFolder()` may throw? It returns null cancel. Fine.

Potential issue: `corpusSessionResSchema` allows `dir` arbitrary string but only from main. OK.

Potential issue: `api-surface.ts`: Channel name `export/corpus-session` but service file comment says "export/corpus-session 通道" good. Event channel `exportCorpus` exists? Need not diff, but references `EVENT_CHANNELS.exportCorpus` in bootstrap. The schemas export ExportCorpusEvent. Need check existing EVENT_CHANNELS has exportCorpus? likely from AI-02. OK.

Potential issue: In `services/index.ts`, `createCorpusExportService` is spread `...createCorpusExportService({...})` into export_. The object has `exportCorpusSession` and `corpusItem`. `ExportService & CorpusExportService` works. But `createCorpusExportService` is instantiated with `sendEvent: deps.sendExportEvent ?? (() => undefined)`. If bootstrap injection missing, sendEvent no-op, session never emits events, but IPC still works. However bootstrap injects. Default no-op maybe hides misconfiguration; but work order says default no-op to keep services/index pure? "sendExportEvent 注入默认 no-op——bootstrap 覆盖" documented. OK.

Potential issue: In services/index.ts, `createCorpusExportService` receives `repos: deps.repos` and `fileStore: deps.fileStore`. `ServiceDeps` includes `fileStore`? Need existing. Probably.

Potential issue: New file interface-template.ts 47 lines; static template uses backticks with `${...}`? It has no interpolation. Good.

Potential issue: Does `INTERFACE_MD` contain backtick inside template literal? It uses `\`INTERFACE.md\`` to escape backticks? Actually string is template literal with backticks; inside it includes backtick sequences `` \`INTERFACE.md\` `` to produce `INTERFACE.md`. Fine. It also contains `${paperId}`? Wait inside template literal, any `${` sequences might be interpreted as interpolation. Let's inspect template content: It includes "corpus/<paperId>.md" etc. In a template literal, `${paperId}` is an expression placeholder! Did they escape? The code diff shows inside template string:

```
- \`corpus/<paperId>.md\`：每篇语料...
```

This is inside a template literal delimited by backticks. `${paperId}` will be evaluated as JS expression. Since paperId is not defined in that module, this would throw ReferenceError at module load! Let's examine carefully. In interface-template.ts, they define:

```
export const INTERFACE_MD = `# Synapse 语料导出接口说明书（INTERFACE）

> ...

- \`INTERFACE.md\`：本说明书
- \`manifest.json\`：...
- \`corpus/<paperId>.md\`：每篇语料...
...
- \`fulltext/<paperId>.txt\`：...
- \`figures/<paperId>/page-<N>.png\`：...
```

Yes! In template literal, `\`...\`` are escaped backticks but `${` is still interpolation. There is no `\${`. So `corpus/<paperId>.md` inside template literal will evaluate `${paperId}`. Unless the template uses `\${` to escape? The diff text shows:

```
+- \`corpus/<paperId>.md\`：每篇语料（结构化标注+笔记，二阶消费主面）
```

Actually in plain text, this is inside a template literal in source, but the diff displays the raw source: it shows backslash-backtick? Let's parse: Source line in file likely starts with `- \`corpus/<paperId>.md\`：每篇语料...` because the diff unmodified? The line from template source: The template literal itself is opened with backtick. To include a backtick in the content, you escape `\``. In the raw source, the line would literally be `- \`corpus/<paperId>.md\`：...`. Then `${paperId}` remains interpolation. Does Node/JS evaluate? Yes, `${paperId}` in a template literal is a substitution. `paperId` is not defined in module, causing ReferenceError at module load. But maybe the source line uses `\${` to escape? The diff display: In provided diff, I see:

`+- \`corpus/<paperId>.md\`：每篇语料（结构化标注+笔记，二阶消费主面）`

Is there a backslash before `$`? The string as pasted: "`corpus/<paperId>.md`" no. Let's look exactly at user content:

```
+- \`corpus/<paperId>.md\`：每篇语料（结构化标注+笔记，二阶消费主面）
```

The raw markdown: +- backslash backtick corpus/<paperId>.md backslash backtick. No backslash before $. So yes, this is a critical bug! The module would fail to load with ReferenceError: paperId is not defined. But brief says verify passed with 69 files 409 tests incl new tests importing service. How could it pass? Wait maybe the test imports `createCorpusExportService`, which imports `INTERFACE_MD`; if module load fails, tests fail. But maybe this diff is not actual final? Could be presentation artifact? Need consider: In the diff given, the line starts with `+-`? Actually plus sign means added line, then `-`? Wait the line in context: interface-template.ts new file. The diff output uses `+` for each added line. So line content starts with `+ - \`corpus...`? Wait the diff we are given:

```
+/**
+ * INTERFACE.md 静态模板单源（AI-03 交付面）。
...
+export const INTERFACE_MD = `# Synapse 语料导出接口说明书（INTERFACE）
+
+> 本目录由 Synapse 应用导出（五件套：本文件+manifest.json+corpus/+fulltext/
+> figures/）。「manifest.json 存在」= 导出完整就绪（唯一激活判据）；导出中断
+> = 无 manifest，重跑导出即修复（目录清空重建）。
+
+## 目录结构
+
+- \`INTERFACE.md\`：本说明书
+- \`manifest.json\`：导出索引（schemaVersion/exportedAt/papers[]/可选 errors[]；
+  contentSha/fulltextSha=对应文件字节 sha256，供增量对比）
+- \`corpus/<paperId>.md\`：每篇语料（结构化标注+笔记，二阶消费主面）
+- \`fulltext/<paperId>.txt\`：全文本层（pdf 文本按页拼接，**页界符 \\f 分隔**，
+  页序 1 基——一阶消费主面）
+- \`figures/<paperId>/page-<N>.png\`：页级快照（N=1 基页码；2 倍分辨率渲染）
+- \`figures/<paperId>/anno-<annotationId>.png\`：标注区域裁剪（归一化 rects
+  包围盒从页快照裁出，与 corpus md 引文块对应）
```

Yes! This is definitely a bug unless `paperId` is defined globally? No. However, wait: The template literal contains `${paperId}` and `${annotationId}` undefined. This would cause ReferenceError when module is evaluated. But maybe TypeScript compiles fine? At runtime, module import fails. Yet work order says `npm run verify` green with 69 files 409 cases including tests that use createCorpusExportService, and corpus.export.test imports createCorpusExportService, which imports `./corpus.assemble` and `./interface-template`; if interface-template module throws, test import fails. Unless vitest doesn't execute top-level? It does. So verify couldn't be green. But perhaps there is a `\` before `${` in actual raw source but hidden in the prompt? Let's examine the displayed line again in user message:

```
+- \`corpus/<paperId>.md\`：每篇语料（结构化标注+笔记，二阶消费主面）
```

Actually in the user's message, for line "corpus/<paperId>.md", there is no backslash-dollar. The preceding line "contentSha/fulltextSha=对应文件字节 sha256，供增量对比）" no. The next line "fulltext/<paperId>.txt" also. "anno-<annotationId>" also. And earlier in side comments there is `figures/<paperId>/page-<N>.png` also. The same issue in file comments? Comments are not template literal? But interface-template content is template literal.

Could the user have mangled backslashes in diff display? For example `\`` appears in raw source. If they had `\${paperId}` in raw source, the diff would display `\${paperId}` with backslash before $. The displayed text would be `\${paperId}`. It isn't. So it's a real bug.

But wait, in template literals, `${` starts interpolation even inside escaped backticks. Yes. So this is a blocking issue: module load ReferenceError. How did tests pass? Maybe the test is "guardedDescribe" or tests not run? But verify includes all tests. Could the new test file not actually import? It imports `createCorpusExportService` from service; service imports interface-template. If interface-template module fails, tests cannot run. The diff's test file includes `import { createCorpusExportService } from '../../../src/main/services/export_/corpus.export.service'` ; service imports `INTERFACE_MD` from './interface-template'. So yes. This is a severe bug. Unless TypeScript's template literal with `\`` doesn't parse `${}`? Let's verify with JS: `` `a \`b ${x} c\`` ``? Actually to include backtick you escape, but `${x}` inside is evaluated. Example: `` `\`${1}\`` `` => "`1`". So yes.

Could the prompt's diff have line `+- \`corpus/<paperId>.md\`：...` where there is a backslash before `$` hidden by markdown? The user pasted raw text; no.

Let's search diff for `\${` occurrences. I see in interface-template template content: `- \`INTERFACE.md\``; `- \`manifest.json\``; etc. For `<paperId>`, no `\${`. Also there is "**页界符 \\f 分隔**" in diff text: `\\f`? Actually source line `**页界符 \\f 分隔**` in template literal would evaluate `\\f` as backslash f? Wait `\\f` in template literal yields `\f` (backslash + f), not form feed. Good. But if they intended `\f` literal, they escaped backslash. OK.

Additionally in test "fulltext 页界 \\f" assertions use `expect(ft1).toBe(\`TEXT-A p-1\fTEXT-B p-1\`)`? In diff test lines:

```
+      const ft1 = await readFile(join(h.dir, 'fulltext', 'p-1.txt'), 'utf8')
+      expect(ft1).toBe(`TEXT-A p-1\fTEXT-B p-1`)
```

In template literal, `\f` is form feed character, which matches service `'\f'` in JS string. Good.

But in interface template, `**页界符 \\f 分隔**` inside template literal: `\\f` produces literal `\f` (backslash + f), which is desired in markdown to show `\f`. OK.

So the interface-template bug is real and critical. But maybe the raw source in actual commit used `\${` but diff parser swallowed backslash? Let's consider: If source had `\${paperId}`, the diff line would be `+- \`corpus/\${paperId}.md\`：...`. The prompt would show `\${`? The user text would include `\${` unless Markdown interprets `\` before `$`? In Markdown, backslash before `$` is not an escape unless `$` is math? But plain text. It would show `\${`. Not present.

Could it be that the template literal is not actually a template literal? `export const INTERFACE_MD = ` at end? Yes.

Thus one BLOCKING finding: interface-template.ts template literal unescaped `${paperId}`/`${annotationId}` -> ReferenceError. Actually first expression `${paperId}` at line - `corpus/<paperId>.md`; module load would throw. This would make `npm run verify` fail, contradicting brief. But audit should report. Severity BLOCKING.

Need see if any other `${}` in template: "front-matter（... **不含 exportedAt**..." no. "figures/<paperId>/anno-<annotationId>.png" yes. "papers[]/可选 errors[]" no. "contentSha/fulltextSha" no. So at least two expressions.

But wait, what if `paperId` and `annotationId` are global? No.

Let's continue other findings.

Potential issue: `corpus.export.service.ts` file header comment uses `INTERFACE.md（interface-template.ts 静态单源，INV-11）` but in AGENTS INV-11? Not listed in diff but likely. Fine.

Potential issue: The `manisfest` JSON includes `schemaVersion: 1` but `interface-template` says "schemaVersion=1; 新增字段必须可选" etc. Good.

Potential issue: `export.service.ts` uses `existsSync` from node:fs sync in async function. Minor performance; not issue. But could be TOCTOU? Guard only checks manifest before write. If another process creates manifest during export? Not concern. If manifest exists but is stale? It rejects. Could be too aggressive if user wants overwrite lightweight export into existing corpus dir; but feature decided. OK.

Potential issue: In `corpus.export.service.ts` file header still says "文件 ≤500" etc.

Potential issue: `orderAiNotes` map doesn't include `annotationId`? `AiNoteEntry` maybe has source/content/page only. Fine.

Potential issue: `questionOrder` uses `Number(q.replace('Q',''))`; for `''` returns 0. Could `divergence` handled. OK.

Potential issue: `SessionError` extends Error; `AppErrorCode` includes 'IO_ERROR'? Need check app-error codes. In diff, app-error codes include 'UPSTREAM_ERROR', 'PARSE_ERROR', 'CANCELLED', 'INTERNAL'. Does it include 'IO_ERROR'? The added codes? We only see part of app-error diff: context includes `UPSTREAM_ERROR`, `PARSE_ERROR`, `CANCELLED`, `INTERNAL`, and added `EXPORT_BUSY`. Is `IO_ERROR` in the code union? The service uses `new SessionError('IO_ERROR', ...)` as AppErrorCode type. But the diff shows AppErrorCode type lines? Let's inspect diff:

```
@@ -22,6 +22,7 @@ export type AppErrorCode =
   | 'UPSTREAM_ERROR' // 上游返回异常数据
   | 'PARSE_ERROR' // 解析失败（PDF/PDF 元数据/BibTeX 上游 JSON）
   | 'CANCELLED' // 用户取消（如关闭对话框）
+  | 'EXPORT_BUSY' // 导出会话单飞：进行中拒绝第二会话（INV-18，AI-03 落地）
   | 'INTERNAL' // 未预期错误（兜底）
```

This diff only shows lines from around 22 onward. It doesn't show all codes before. There may be 'IO_ERROR' already. The code const list after:

```
const APP_ERROR_CODES: readonly AppErrorCode[] = [
+  'EXPORT_BUSY',
  'NOT_IMPLEMENTED',
  'INVALID_REQUEST',
  'NOT_FOUND',
```

Again doesn't show IO_ERROR but could be later. Need check if IO_ERROR exists elsewhere in code? The export.service uses `ExportDomainError` maybe with codes like IO_ERROR. Workorder says SessionError code toAppError. Not enough to flag.

Potential issue: `failSession` uses `new SessionError('IO_ERROR', message)` but `corpusItem` catch if ENOTDIR fails, failSession rejects session with IO_ERROR but `corpusItem` rethrows the original ENOTDIR. IPC handler may convert to AppError 'IO_ERROR'? Not sure. But if `toAppError` only recognizes AppErrorCode list, if IO_ERROR absent, compile error would catch. So likely exists.

Potential issue: In `corpus.export.service.ts`, `let created: ActiveSession | null = null` then `const active = created as unknown as ActiveSession`. This is ugly type assertion. They could initialize inside Promise executor but need session before async. Because `new Promise` executor runs synchronously, they could assign. But they cast null as ActiveSession. This is type abuse? It's safe because executor synchronous. But `created` definitely assigned. NIT/type smell. Could use let active: ActiveSession; new Promise(... active = ...). But not blocking.

Potential issue: `corpusItem` catch: if `finishPaper` throws `SessionError('INTERNAL')` due to detail null, catch calls `failSession` (rejects session) then rethrows SessionError. But `finishPaper` may have already advanced and changed `s.current`; failSession sets session null. OK.

Potential issue: `corpusItem` returns `{ok:true}` even if req.kind is error? Actually for error branch calls finishPaper then returns ok. OK.

Potential issue: `corpusItem` for `kind: 'figure'` with `req.annotationId` undefined and `req.figure === 'anno'` uses `'unknown'`; but schema maybe annotationId optional. Could write `anno-unknown.png` for each unknown. OK.

Potential issue: base64 payload validation? It writes Buffer.from(payload,'base64') which can contain garbage; no schema? `corpusItemReqSchema` probably validates kind, payload string. Fine.

Potential security: `dir` from main dialog, but service writes files using ids from DB. If `paperIds` list includes `../../x` and DB detail null, it pushes error and continues, never writes file. So no path traversal unless a DB row id contains path separators. Could DB id be user-controlled via import? Let's look at paper import: likely generates id using crypto.randomUUID. So no. But `paperId` in `corpusItem` must match current.paperId, so safe. `annotationId` could be from renderer req in `corpusItem` kind figure with figure 'anno'; the req.annotationId is not validated against current annotations. Schema allows `annotationId?: string`. If renderer sends `../../evil` as annotationId with figure 'anno', path could be `figures/p-1/anno-../../evil.png`; `join` normalizes to `figures/p-1/evil.png`? Let's see `join(s.dir,'figures',cur.paperId, `anno-${req.annotationId ?? 'unknown'}.png`)`. If annotationId = '../../x', name = 'anno-../../x.png'. join(dir,'figures','p-1','anno-../../x.png') normalizes: dir/figures/p-1/anno-../../x.png -> dir/figures/x.png? Actually `path.join` will resolve `..` segments: segments: [dir,'figures','p-1','anno-..','..','x.png'] after splitting? Wait `'anno-../../x.png'` is a single segment "anno-../../x.png" containing slash. `path.join` processes segment as "anno-../../x.png": it sees "anno-.."? Actually string includes `..` after `anno-`? `anno-../../x.png`: characters "anno-" + ".." + "/" + ".." + "/x.png". So the segment is "anno-..", then "..", then "x.png". join(dir,'figures','p-1','anno-..','..','x.png') -> dir/figures/x.png? Let's compute: dir/figures/p-1/anno-.. -> dir/figures/p-1/..? Wait segment "anno-.." is literal, not special because it's not exactly `..`. Then next `..` pops to dir/figures/p-1? Actually joining: base dir, figures, p-1, anno-.., .., x.png => dir/figures/p-1/anno-../..? Path segments: after p-1, `anno-..` literal; `..` goes up to p-1? Wait "anno-.." is not `..`, so final path = dir/figures/p-1/anno-../x.png? Let's parse: "anno-../../x.png" as one segment is invalid? On Windows, path separator `/` within segment splits. `path.join` string argument one segment "anno-../../x.png": The OS path parser treats "anno-.." as first component, then "..", then "x.png". So after p-1, next component "anno-.." (normal), then ".." pops back to p-1? Let's use algorithm: start stack [dir, figures, p-1]. Add "anno-.." => [..., anno-..]. Add ".." => pop last ("anno-..")? Actually path.posix.join resolves "." and ".." relative to current: after "anno-..", then ".." removes "anno-.." and returns to p-1. Then "x.png" => dir/figures/p-1/x.png. So it doesn't escape outside figures/p-1. If annotationId = '../../../evil', segments: "anno-..", "..", "..", "evil.png" => after p-1: add anno-.., .. pops anno-.., .. pops p-1 => dir/figures/..? Wait stack after p-1: [dir,figures,p-1]. Add anno-.. -> [dir,figures,p-1,anno-..]. Add .. -> pop anno-.. -> [dir,figures,p-1]. Add .. -> pop p-1 -> [dir,figures]. Add evil.png -> dir/figures/evil.png. So `annotationId = '../../../evil'` yields figures/evil.png. Still within figures but not paper subdir; `figures` dir could be polluted. More importantly, `annotationId = '../../../../../../evil'` could escape to dir/evil.png or outside? With enough `..`, path.join normalizes to filesystem root? For each `..` beyond root, path.join may keep going up? On POSIX, path.resolve treats `..` above root as root; join maybe retains? Let's test mentally: path.posix.join('/tmp/foo','figures','p-1','anno-..','..','..','..','evil') -> /tmp/evil? Actually /tmp/foo/figures/p-1/anno-.. -> pop to p-1; second .. -> figures; third .. -> foo; fourth .. -> tmp; then evil -> /tmp/evil. Yes can escape the export dir! Because annotationId can be "../../../.." plus "evil" in same string? Need string: `anno-${req.annotationId}.png` gives "anno-.." as first chunk, then user's `..` chunks. So if annotationId = '../../../evil', components: "anno-..", "..", "..", "evil.png" (since suffix .png appended after base? Actually string `anno-${annotationId}.png`; if annotationId `../../../evil`, name = `anno-../../../evil.png`. Split by slash: ["anno-..", "..", "..", "evil.png"]). Stack from dir/figures/p-1: add anno-..; pop with .., pop p-1 with second ..? Let's list: base [dir, figures, p-1].
component "anno-.." -> stack + anno-..
component ".." -> remove last: removes anno-.. (stack back to p-1)
component ".." -> remove last: removes p-1 (stack [dir, figures])
component "evil.png" -> [dir,figures,evil.png]
So only escapes p-1, not figures. Need more `..`: `annotationId = '../../../../evil'` => "anno-..", "..", "..", "..", "evil.png": after anno-.. pop; pop p-1; pop figures; then evil -> dir/evil.png. So can overwrite arbitrary file in export dir root. More `..` can escape dir. Since `dir` is user-selected temp/export directory, and annotationId comes from renderer (potentially compromised or malicious because webContents could be XSS). But renderer is trusted? Still security "禁止 renderer 引入绝对路径", but this is main receiving a relative path-ish value and using in path join without validation. That's a path traversal vulnerability! Similarly `page` number could be negative? It's number, used as `page-${req.page}.png`; if req.page = `..`? Schema likely page number. If payload? No. `paperId` in corpusItem must match current.paperId; current from DB. So not. `annotationId` optional in schema can be arbitrary string. The code should validate annotationId to known annotation IDs or sanitize to basename / use safe naming. This is a real security finding (path traversal) though impact limited to inside export directory or outside if enough `..` and permissions. Need decide severity BLOCKING due to security. The audit dimension says path traversal BLOCKING. Even if renderer is local app, security hard rules: renderer shouldn't control paths. This is a main-side path construction with renderer-controlled annotationId. Also `req.page` could be `..` if schema is number? If z.number, no. But `annotationId` string no pattern. Also `figure` field enum? maybe.

Also for figure kind 'page', filename `page-${req.page}.png`; if req.page can be string? `corpusItemReqSchema` likely from AI-02 maybe page z.number(). Need check not in diff. We can flag annotationId interpolation. The extractor AI-02 likely sends annotation IDs from its own list. But defense should validate. In code, they could check `req.annotationId` against current annotations? They don't have list. At least use regex replace or use only filename-safe.

Similarly `paperId` from corpusSessionReqSchema used in path after `detailById` and detail.fileName? Actually `writeFile(join(input.dir,'corpus', `${id}.md`))` where id from input, but detail must exist. Could a malicious DB row have id `../../evil`? Probably not, but if import service allows filenames? Paper ID likely UUID. Not flag.

But path traversal via annotationId is significant. Need see schema in diff? corpusItemReqSchema not in diff; maybe existing from AI-02. The audit can reference code in diff: `name = req.figure === 'anno' ? \`anno-${req.annotationId ?? 'unknown'}.png\` ...` and `join(figDir, name)`. This is in diff. So we can report.

Security also: `payload` base64 writes arbitrary file content; if annotationId outside, can write arbitrary file. User-controlled base64 from renderer. But renderer is app code; still.

Potential issue: `cleanRebuild` uses `rm(join(dir, sub), { recursive: true, force: true })`; `dir` from main dialog, fine. If service called in tests with arbitrary dir? Not security.

Potential issue: `exportCorpusSession` with `paperIds` unlimited? schema max 1000. OK.

Potential issue: Test for path traversal absent. Could suggest add.

Potential issue: In `corpusItem`, if `req.kind === 'figure'` and `req.figure === 'anno'`, `annotationId` might be undefined -> 'unknown'. If malicious, path traversal. We should include as BLOCKING security.

Potential issue: `fileStore.resolveManagedPath` in tests stub maps fileRef to `join(dir,'files',fileRef)`. It returns arbitrary if fileRef. In service, `resolveManagedPath(detail.fileName)`. If detail.fileName from DB could be path traversal? Import likely sanitizes to basename. Not in diff.

Potential issue: In test harness, `fileStore.resolveManagedPath` typed `Pick<FileStore,'resolveManagedPath'>`, but actual FileStore method signature maybe `resolveManagedPath(fileName: string): string`. OK.

Potential issue: Test uses `createTestDb` from fixtures; does it apply migrations? They mention "openDatabase 无迁移→createTestDb". OK.

Potential issue: The new test file uses `guardedDescribe` likely okay.

Potential issue: `corpus.export.test.ts` line count 374 including imports, okay.

Potential issue: `orderAiNotes` sorting comparator returns 0 when `a.createdAt` equal and `a.id` equal? IDs unique, so no. But TypeScript sort comparator must return number; they return -1/1. OK.

Potential issue: `questionOrder` for q `'divergence'` returns 99; if `Number` returns NaN? `Number('divergence')` NaN but they special-case before. Good.

Potential issue: `ROLE_ORDER` uses role as index; if role invalid, undefined-undefined NaN. Type.

Potential issue: `app-error.ts` added EXPORT_BUSY to union and list. If APP_ERROR_CODES order? fine.

Potential issue: `locks/manifest.json` generatedAt changed, app-error, api-surface, schemas, new test. But test file is locked; added. Did they run `locks:apply`? Manifest includes new test. Not check whether they modified `tests/**` without unlock? The constitution says tests/shared locked; test new file is new, adding locked file requires `locks:generate` before apply. Manifest includes it with sha. OK. They didn't modify existing tests. New test in diff is being added with manifest. OK.

Potential issue: They modified `src/shared/app-error.ts`, `src/shared/ipc/api-surface.ts`, `src/shared/ipc/schemas.ts` [受锁] with comment. The diff header doesn't show `[locked-change]` in commit message but we don't see commit. Work order notes "schemas 增 ... [受锁]". Could be okay. AGENTS says locked files must be changed with locks:unlock and apply; manifest updated. Fine.

Potential issue: `src/shared/models/ai-note.ts` not in diff, no.

Potential issue: `corpus.assemble.ts` header maybe "file ≤500"? not.

Potential issue: `interface-template.ts` file is new but not added to locks manifest? Wait manifest diff adds only test file, not interface-template. Is interface-template locked? It's in src/main/services, not locked unless main services locked? Manifest has src/shared, tests, migrations, CI, lint, build config. src/main not generally locked. Fine.

Potential issue: "受锁 e2e spec 改动后全量 verify" no e2e.

Potential issue: New `interface-template.ts` not referenced? It is imported by service. OK.

Potential issue: `corpus.export.service.ts` line count: Original stub path shows line 103+270 additions = about 340? Could be under 500. Let's approximate diff: The new file after changes likely 370 lines. OK.

Potential issue: "repo ≤300 行" no.

Potential issue: `bootstrap.ts` `sendExportEvent` broadcasts to all windows. OK.

Potential issue: `CorpusExtractor.ts` comment change only. But work order says check-tickets short-form references. Not issue.

Potential issue: In schemas, `corpusSessionReqSchema` comment says "目录经 main 侧系统对话框（INV-07）service 内选" but actual ipc picks dialog, service receives. This is a documentation inaccuracy. Could be NIT/WARN? It might confuse. Also schema `paperIds` max 1000 but work order says full library default. Fine.

Potential issue: `corpusSessionResSchema` `dir: z.string()` no min. Fine.

Potential issue: `api-surface.ts` line length maybe >100? Check line:
`corpusSession: { channel: 'export/corpus-session', Req: S.corpusSessionReqSchema, Res: S.corpusSessionResSchema }` likely long >100. Existing style? Could fail lint. But brief says lint green. Not flag.

Potential issue: In `corpus.export.service.ts`, `const active = created as unknown as ActiveSession` after Promise executor. Since `created` type is `ActiveSession | null`, and `Promise` executor is synchronous by spec. It's ugly but not a runtime bug. Could be NIT.

Potential issue: If `deps.sendEvent` throws, sessions fail? sendEvent in bootstrap maybe `webContents.send` can throw if destroyed. `sendProgress` calls sendEvent without try/catch; if window destroyed, webContents.send may throw? Electron's send on destroyed webContents may throw? It could cause session fail incorrectly. But not in diff? They should wrap? Existing sendProgress probably similar. Minor.

Potential issue: `startPaper` doesn't check that `paper` file still exists? It prepared before; if file removed during session, `startPaper` sends extract request, renderer may fail to open and return error. OK.

Potential issue: `cleanRebuild` before preparing writes INTERFACE.md; if a user had INTERFACE.md with custom modifications, it overwrites. That's intended.

Potential issue: Manifest `exportedAt` uses now at session final, but per-paper exportedAt uses now at finishPaper time. Both same now injection. OK.

Potential issue: INV-17: contentSha should equal file bytes sha256; code reads corpus md file and hashes utf8 content, then writeFile wrote same string with utf8. Node writeFile with utf8 and readFile with utf8 returns same string; `createHash.update(string,'utf8')` hashes UTF-8 bytes. Good. FulltextSha uses in-memory fulltext string but after writeFile same bytes. Good.

Potential issue: The service writes `fulltext` with `cur?.fulltext.join('\f')`; if no fulltext chunks, write empty. Does "fulltext" maybe should include page breaks? OK.

Potential issue: Does fulltext text need to escape `\f`? no.

Potential issue: The `finishPaper` uses `cur?.fulltext` but `cur` could be null if called for error? Error branch doesn't use cur. For complete branch, cur non-null. OK.

Potential issue: If `corpusItem` receives kind 'fulltext' after figure or complete? It doesn't enforce order. Could duplicate. Not severe.

Potential issue: If extractor sends multiple figure for same annotation id, manifest duplicates figures path. Not checked. Could cause duplicate entries. Not severe but maybe should dedupe? Not in spec. Could be WARN? No.

Potential issue: `fileCount` is number of papers not number of files. Name maybe ambiguous. Existing C-02 `fileCount` likely count entries. OK.

Potential issue: In test "正常全链", `r1 = await nextExtract(h, 0)`; then if r1.paperId is 'p-1', they expect annotations ['a-1']; else not. If first is p-2, they don't assert annotations for p-1. Fine.

Potential issue: Test "EXPORT_BUSY" line `await emulateExtractor(h, (await nextExtract(h, 0)))`; after first session began, nextExtract returns first extract. `emulateExtractor` sends figures for annotations (none) and complete, resolves first. OK.

Potential issue: Test "落盘失败" after fail, `retry = h.svc.exportCorpusSession({ dir: h.dir })`; `nextExtract(h, 1)` waits for second extract-request. But events already contain first extract-request and then after retry cleanRebuild, prepare write corpus, advance sends second extract. OK.

Potential issue: Test "orderAiNotes" doesn't assert createdAt/id tiebreak. Not required.

Potential issue: Missing test for finalizing manifest atomic no window? They assert manifest exists after. Fine.

Potential issue: Missing test for "INTERFACE.md 静态单源" content? They only assert file exists. Could be WARN? Not necessary.

Potential issue: `interface-template.ts` says "manifest.json 存在=导出完整就绪" but `manifest.json` can contain only errors, no papers. Still ready? Maybe.

Potential issue: In `export.service.ts`, guard uses `existsSync` synchronous before try; if `dir` doesn't exist, exists false; then mkdir and write. OK.

Potential issue: `writeCorpusSet` guard checks only `manifest.json`, not `manifest.tmp.json`. If interrupted session left tmp and no manifest, lightweight export allowed into tmp? It would create corpus dir and files, but no manifest, so tool not activated. Could leave tmp. But no pollution of active manifest. OK.

Potential issue: `cleanRebuild` does not remove `figures` root? it removes and recreates. Good.

Potential issue: The session's `promise` may never settle if `advance` throws after `session=null`? Let's inspect failSession paths carefully.

In exportCorpusSession:
```
try {
 await cleanRebuild(input.dir)
 for ... { await writeFile ... }
 await advance(active)
} catch (e) {
 await failSession(active, ...)
}
return promise
```
If `cleanRebuild` or writeFile throws, failSession sets session=null and rejects. OK.
If `advance` synchronously throws before any await? `advance` is async. It calls `s.queue.shift()`, if next startPaper; startPaper sends event maybe no throw; if next undefined finalizing writes files. If `sendEvent` throws in startPaper, advance rejects, catch failSession. OK.

corpusItem:
```
try {
 if fulltext push
 else if figure { mkdir; write; push}
 else if complete { ... await finishPaper(...) }
 else { await finishPaper(...) }
} catch(e) {
 if INVALID_REQUEST throw
 await failSession(s,...)
 throw e
}
return {ok:true}
```
Suppose `finishPaper` (complete) calls `advance` which starts next paper. If `sendEvent` throws in startPaper, finishPaper rejects, catch failSession. Session reject. OK.
Suppose `finishPaper` writes manifest and resolves session, then finishPaper returns. No catch. OK.

What if `failSession` called after session already resolved? Could happen if renderer sends a duplicate complete/error after session already done? Let's think: After final paper complete, `finishPaper` calls advance finalizes and resolves, session=null. corpusItem returns {ok:true}. If renderer sends another fulltext for same paper after complete? At that moment `s.current` was set null in finishPaper; session=null? Actually `session=null` set in advance during finalizing before resolve. Then subsequent corpusItem sees s null -> INVALID_REQUEST. It throws INVALID_REQUEST, catch checks code and rethrows, no failSession. OK.
What if after a non-final complete, advance starts next paper and s.current becomes next. Late response for previous paper: s.current.paperId mismatch -> INVALID_REQUEST. OK.
What if `finishPaper` ok:false branch pushes error and calls advance; if no next, finalizes and resolves; then duplicate error for same paper? At beginning of finishPaper it set `s.current=null` and `s.done+=1`; if earlier a complete came for same paper? No current would be null so INVALID_REQUEST. OK.

Potential issue: `s.done` increments for failures both in preparing and finishPaper. But `total` is ids.length; done counts only processed. OK.

Potential issue: In preparing failure (paper missing) `active.done += 1` but doesn't write Corpus. `done` not reflected in sendProgress except finalizing? OK.

Potential issue: `sendProgress(s,'streaming')` sends total/done; done may not include current paper until complete. Fine.

Potential issue: `errors` in manifest include reason "文献记录不存在" but paperId from input; if paperIds invalid, manifest has errors with non-existent id. OK.

Potential issue: If `paperIds` is empty array? schema min(1), but service if input.paperIds ?? listAllIds; if empty array somehow from tests, ids.length 0, session finalizes empty. OK.

Potential issue: "单会话单飞" but `exportCorpusSession` sets `session` after creating promise. The promise executor runs synchronously, so `created` set. However `created as unknown as ActiveSession` uses `unknown` because couldn't assign? Could instead use `let active!: ActiveSession`. Minor.

Potential issue: If `new Promise` executor throws (e.g., creating object with Math.random? no), error thrown synchronously, but no catch? Not.

Potential issue: TypeScript strict? OK.

Potential issue: `SessionError` does not set `name` or prototype? Extends Error works. OK.

Potential issue: In `interface-template.ts`, if `${paperId}` bug exists, the module would fail at import. This is more severe than path traversal. Need decide whether to mention both as findings. Yes.

But wait: Could TypeScript compile a template literal with undefined variable inside expression? It would error "Cannot find name 'paperId'" at compile time. If `npm run verify` includes typecheck, it would fail. So maybe the actual source has `\${`. Let's reconsider: The user's diff text might have Markdown rendering that removes backslashes? In the plain text, lines are inside a code block? Actually user used code blocks without markdown? The diff is in a code block? The user message: after "diff --git a..." they have raw diff with lines. No markdown escaping. If source had `\${`, it would appear. However maybe in the diff, a line `+- \`corpus/<paperId>.md\`：...` the backslash before `$` is not visible because it's consumed by Markdown when rendering? In Markdown, a backslash before `$` is not a standard escape; it would show. Also in code block, all backslashes preserved. So likely no escape.

But then how could brief's "npm run verify" green be true? Maybe `interface-template.ts` template literal is not the actual source; perhaps they intended but didn't run? The brief says implemented and verify green. The prompt may be a synthetic audit scenario with a planted bug. The audit should catch it. Good.

Let's inspect the exact template line in diff:
`export const INTERFACE_MD = \`# Synapse 语料导出接口说明书（INTERFACE）`
Actually the opening backtick in diff is escaped as `\``? Wait diff line: `+export const INTERFACE_MD = `# Synapse 语料导出接口说明书（INTERFACE）`. In the user's message, it appears as backtick before #. The diff display doesn't escape it. Then subsequent lines with backticks are escaped as `\`` because inside template. But the `${` not escaped. Yes.

So finding: BLOCKING line_hint "interface-template.ts line 33/36/39?": template literal unescaped `${paperId}`/`${annotationId}` causes ReferenceError at module load and typecheck failure. Suggestion: escape as `\${paperId}` or use String.raw? Actually use `\${` to include literal `${`.

Also check if `interface-template.ts` has other `${` in "contentMd"? no.

Now, potential issue with "orderAiNotes" mapping and `question` type. `AiNoteQuestion` maybe includes 'divergence' and `Q1`..`Q7`; `questionOrder` with `Number(q.replace('Q',''))`: for "Q1" -> 1; "Q7" -> 7; "divergence" -> 99. OK.

Potential issue: In `corpus.assemble.ts`, the `AiNoteEntry` interface already had `source`,`content`,`page`. `OrderedAiNoteEntry` extends with `sourceRole`,`question`. `assembleCorpusMd` input uses `aiNotes` type maybe `AiNoteEntry[]`, so Ordered entries can pass. OK.

Potential issue: The comment in corpus.export.service "renderer 篇 error 接续" etc.

Potential issue: `corpus.export.service.ts` header says "IPC [受锁]：export/corpus-session 通道（Req corpusSessionReqSchema/Res corpusSessionResSchema）". Good.

Potential issue: In `ipc/export_.ts`, `corpusSession` handler uses `deps.dialogs.pickFolder()` without args. Does `pickFolder` exist in IpcDeps? Diff uses it, compile would verify. OK.

Potential issue: `ExportIpcError('CANCELLED', ...)` maybe not a defined class? Diff uses; compile would verify. OK.

Potential issue: The IPC handler doesn't check `req.paperIds` undefined? It passes to service. Schema validates. Service default full library. OK.

Potential issue: The directory guard in `export.service.ts` uses `existsSync` but not `await`. The file already imports `existsSync`. It is sync in async path, but acceptable. Could use `access` but minor.

Potential issue: In `corpus.export.test.ts`, they import `createExportService` from export.service and use it to test guard. But `createExportService` is a different service with same dir; the guard is in service, not in IPC. OK.

Potential issue: Test "目录隔离守卫" passes `entries = [{ paperId: 'p-1', content: 'md' }]`; need type? likely. OK.

Potential issue: Test "落盘失败" uses `session` as promise but never awaits before creating directory? They await nextExtract, so preparing complete. OK.

Potential issue: Test "清空重建" writes user-notes file and expects preserved. Good.

Potential issue: Test "missing files" uses `h.missingFiles.add('p-bad.pdf')` after seed. But `seedPaper` writes actual file at `files/p-bad.pdf`; fileStore maps to missing if missing set. So readFile fails. Good.

Potential issue: The test file is not listed in `locks/manifest.json` with not generated? It is.

Potential issue: `locks/manifest.json` has generatedAt "2026-08-25T23:04:16..." while docs says date 2026-08-27. Maybe timezone or lockfile generated later. Not issue.

Potential issue: `docs/invariants.md` updated with "已锚定" for INV-17/18. Good.

Potential issue: In `corpus.export.service.ts` header comment "完成定义" line includes "删除 STUB → npm run verify 绿" but not updated? Let's inspect diff context: The stub header still contains "完成前" maybe:

```
 * - 完成后：删除 STUB → npm run verify 绿 → 人工审查 git diff → 翻 registry
 */
```

This is a leftover STUB instruction in the file after implementation. Actually the entire header is the work order; after implementation, it still says "完成后：删除 STUB". But the stub marker `CORPUS_EXPORT_SERVICE_STUB` was removed. The header comment isn't a code issue but leftover placeholder text. AGENTS DoD: grep no TODO|FIXME|placeholder; this comment says "删除 STUB" but not actual placeholder. Could be considered dead/leftover? Minor NIT.

Potential issue: In `corpus.export.service.ts` header, it says "AI-02 已建 renderer 侧回传通道（CorpusExtractor+export/corpus-item 契约），本占位经 register 折叠 NOT_IMPLEMENTED → renderer failed 路径（链路可测）。" Wait diff shows the large header was partially removed? At top after full comment, the `/**` block includes implementation notes. The original stub comments might remain. Let's read diff around top: The old lines:

```
- * - IPC [受锁]：export/corpus {paperIds?} → {dir, fileCount, errorCount}
- *   （全库默认；会话终局 resolve）；export/corpus-item（AI-02 建通道，本单
- *   main 侧消费流式落盘）；exportCorpus 事件发送器经 bootstrap 装配桶注入
- *   （importProgress/sendProgress 同型先例）
```

Then new lines after "接口层" replaced. Later architecture lines remain. The old stub block:

```
-/**
- * 会话服务工厂（AI-03 实现前占位形态）：corpusItem 消费端随本工单落地——
- * AI-02 已建 renderer 侧回传通道（CorpusExtractor+export/corpus-item 契约），
- * 本占位经 register 折叠 NOT_IMPLEMENTED → renderer failed 路径（链路可测）。
- */
```

removed. New implementation comments fine. But header may still have "完成后：删除 STUB" line at end. Let's check diff at bottom header:

```
 * - 完成后：删除 STUB → npm run verify 绿 → 人工审查 git diff → 翻 registry
 */
```

This line is context, not removed. It was in original stub header and remains after implementation. It says "完成后" but now it's done. Not a placeholder in code; but a stale instruction comment. NIT.

Potential issue: `corpus.export.service.ts` line "SR2-AI-03" stub marker removed. Good.

Potential issue: The service's `exportCorpusSession` async method returns promise but also uses `await advance(active)` at end. If `advance` finalizes and resolves, then `return promise` (already resolved). Good.

Potential issue: If `advance` finds next paper and `startPaper` sends event, then `advance` returns; `exportCorpusSession` returns promise (pending). Good.

Potential issue: If `advance` called while `s.current` is null? In startPaper, s.current set before send. In finishPaper, current set null. advance called after finishPaper, current null. If queue empty finalizing. If next, startPaper sets current. Good.

Potential issue: If multiple `advance` calls are made concurrently? Could happen if two corpusItems complete for same session? For example renderer sends `complete` for paper, and also sends `error` for same paper concurrently; IPC messages handled sequentially? `corpusItem` async but handler invoked per IPC; if two IPC in flight concurrently, both see s.current same? First complete calls finishPaper, sets current null, then startPaper next (current becomes next) before first returns? Actually finishPaper for complete: after write, calls `await advance(s)`; advance starts next (sets current to next) and returns; finishPaper returns; corpusItem returns. If second error IPC for previous paper starts before first complete returns? Because `corpusItem` is async and yields at first await in finishPaper/write; but complete branch has no await before? It computes detail, calls finishPaper, which has awaits (writeFile, readFile, advance). So there is a window where `s.current` is null? Let's track: corpusItem complete enters finishPaper; sets `s.current = null`; awaits writeFile fulltext. During this await, another IPC message (error for previous) could call corpusItem. It sees `s.current === null` -> INVALID_REQUEST. Good, rejects. It doesn't disturb. If second IPC comes after write but before advance? current still null. Good. So safe.

Potential issue: If second complete for same paper while first in flight? current null -> invalid. Good.

Potential issue: If renderer sends figures after fulltext but before complete, they are sequential? Could be multiple in flight. Figure writes don't change current, so OK. Order between fulltext and figure from same renderer likely preserved because IPC messages are ordered per webContents? Electron IPC message ordering? Probably.

Potential issue: If sendEvent no-op default in services/index but in tests sendEvent collects. OK.

Potential issue: `sendProgress` in `advance` finalizing sends `done=s.done, total=total`. After last paper done incremented, done=total. Good.

Potential issue: The `manifest` includes `exportedAt` at session level and per paper. Per-paper exportedAt is same as now at finish time; if session runs across midnight, different. Fine.

Potential issue: `fileCount` in result from session at finalizing is s.papers.length; if some papers still being extracted? finalizing only after all. OK.

Potential issue: If queue is empty but errors exist, finalizing writes manifest with errors. `fileCount=0`. Maybe `fileCount` should be number of written files? Since there are 0 corpus? But INTERFACE.md and manifest exist. If user wants "fileCount" maybe 0. Not issue.

Potential issue: The state machine in implementation doesn't include explicit idle state as object but `session=null`. Fine.

Potential issue: The work order mentions "ticks: BUSY/篇失败/落盘失败/失配防御/清空重建" and tests cover. Good.

Potential issue: "manifest 原子性（tmp+rename）与「存在=就绪」判据是否无窗口？" They use rename; but if crash after rename before resolve, manifest exists but session promise never resolves; process crash doesn't matter. If crash after write tmp before rename, tmp may remain, no manifest. Good. But if crash during `rename` atomic? rename atomic. OK.

Potential issue: If a user opens another session while failSession's `rm` is pending? failSession sets session=null before rm, so new session can start while old tmp removal in progress. Old tmp removal might remove new session's tmp? Let's examine: failSession:

```
async function failSession(s, message) {
 session = null
 await rm(join(s.dir, MANIFEST_TMP), { force: true }).catch(...)
 s.reject(...)
}
```

If failSession called from exportCorpusSession catch, it sets session=null, then awaits rm. The `exportCorpusSession` catch is inside the first call; before the catch finishes, the user could trigger a new session (because session=null already). New session's cleanRebuild also removes tmp. The old rm and new rm may race but both force true. No harm. If new session writes new tmp before old rm executes, old rm could delete new session's manifest.tmp? That's a race! Let's analyze timing:

1. First exportCorpusSession fails during preparing. catch -> `await failSession(active, ...)`. failSession sets `session = null`, then calls `await rm(MANIFEST_TMP)`. Because rm is async, the function yields.
2. While rm in flight, the caller (renderer) could retry; new exportCorpusSession begins. session null, sets new session, cleanRebuild removes MANIFEST_TMP (old, maybe nonexistent), writes corpus, sends extract, etc.
3. If new session reaches finalizing before old rm completes? Old rm was initiated before new session's cleanRebuild; it might execute after new session has written its own manifest.tmp. Since rm is force, it would delete the new session's manifest.tmp, then new finalizing rename would fail ENOENT, causing failSession for new session. This is a race.

Is this realistic? The old failSession's rm path is same `s.dir/MANIFEST_TMP` for same dir (because retry likely same dir). The rm is called before reject, but no await? It is awaited. If failSession invoked from `corpusItem` catch, same. The session is null before rm. A new session can start. The old rm may unlink a newly created tmp. There's a race window. This could violate "重跑修复" semantics and "session release". To avoid, failSession should either not rm asynchronously after releasing session, or keep session non-null until cleanup done, or use a unique tmp name per session. Also even cleanRebuild removes tmp with force while old rm maybe; but new session's finalizing writes tmp after old rm likely done. The order of operations: old rm is initiated and likely completes quickly. New session must do cleanRebuild (awaits many rm/mkdir/write), prepare (write corpus), etc., before finalizing writes tmp. That involves many awaits; old rm likely completes first. But not guaranteed; could be scheduled after. More importantly, failSession's `rm` is not the only cause; `cleanRebuild` of a new session while old session still has in-flight? But session null. Could add unique tmp name like `manifest.${sessionId}.tmp.json` and clean both? Or failSession could await rm before setting session null? But then single-flight holds during cleanup; cleaner. The current `session = null` before cleanup is a race. Is this a real blocking bug? It's an edge but could cause false failure. The test "落盘失败" after fail removes figures then retries; in test, old rm likely done before retry because `await expect(session).rejects` waits for failSession? Wait `session` promise rejects at end of failSession after rm; `s.reject` called after await rm. So by the time `await expect(session).rejects` resolves, failSession has completed and rm finished. Then test removes figures and retries. So no race in test. But in real app, user may retry immediately after seeing failure; renderer won't know promise rejected until failSession completes. The renderer calls exportCorpusSession from IPC; service returns promise; failure surfaces via IPC only after failSession done. The user can't retry before previous IPC rejects because UI knows session is in progress? Actually `exportCorpusSession` promise reject is sent as IPC response; until then, renderer may not know failure, so likely won't retry. But the service sets session=null early; another IPC from a different path could start. Low probability. Still race/hardening. Could mention WARN.

Potential issue: Similarly `cleanRebuild` removes manifest at start; old session failure clean with tmp. Unique tmp names would avoid. Not critical.

Potential issue: `finishPaper` uses `cur?.fulltext`; if current null but outcome ok? Not possible. OK.

Potential issue: The `SessionError` code `INTERNAL` used when detail missing in finishPaper; maybe should not fail entire session? It is internal inconsistency, fail is appropriate.

Potential issue: `corpusItem` when `req.kind === 'figure'` writes file with base64; payload could be huge; no size limits. But extractor expected.

Potential security: Path traversal via annotationId again. Let's see if schema `corpusItemReqSchema` has annotationId pattern. Not in diff. The service should sanitize. Could also `req.figure`? In code `req.figure === 'anno' ? ... : page-${req.page}.png`; if `req.figure` is not 'anno' and not 'page'? The enum maybe. `req.page` if z.number, safe. If `req.page` is `NaN`, template `page-NaN.png`. Not security.

Potential issue: The guard in `export.service.ts` `existsSync(join(dir,'manifest.json'))` blocks lightweight export into five-piece dir, but if dir contains manifest.json but is a regular user file not from export, it also blocks. Could be annoying but decision. OK.

Potential issue: `corpus.export.test.ts` uses real `tmpdir` and rm dispose. If test fails before dispose? finally. Good.

Potential issue: The test "miss files" with `fileExists` false: It still doesn't produce extract request for p-bad. Then first extract is p-ok. Good.

Potential issue: Test "renderer 篇失败" `emulateExtractor(h, ra, {failWith: '提取渲染失败'})` sends error. `finishPaper` ok:false calls advance next; session resolves. Good.

Potential issue: In manifest errors, `reason` from renderer (user-controlled string) included; could be large or contain newline, but JSON fine. Not security.

Potential issue: In `orderAiNotes`, `content` uses `${n.question}: ${n.contentMd}`; if contentMd contains newline, maybe formatting broken. Assemble probably handles. Not.

Potential issue: `corpus.assemble.ts` imports type AiNoteRole etc. It adds `OrderedAiNoteEntry` export. Fine.

Potential issue: "same module R12": orderAiNotes in assemble.ts. OK.

Potential issue: `interface-template.ts` has "schemaVersion/exportedAt/papers[]/可选 errors[]" but manifest JSON has `papers` always (array), errors optional. OK.

Potential issue: `interface-template.ts` says "front-matter（YAML 单引号标量）：... annotationCount / noteCount" but assemble may not include noteCount? Not in diff. OK, not audit.

Potential issue: In `docs/invariants.md`, INV-18 anchor says "e2e 消费方级随 SR2-AI-04" but work order's task is AI-03; okay.

Potential issue: `corpus.export.service.ts` header "文件 ≤500" not.

Potential issue: Dead code: `CORPUS_EXPORT_SERVICE_STUB` removed. Good. `import { NotImpl...}` removed. Good.

Potential issue: In `services/index.ts`, `sendExportEvent` optional with default no-op. Could be considered a placeholder? It's intentional. OK.

Potential issue: `bootstrap.ts` added `sendExportEvent` but did not add to `ServiceDeps`? Services/index uses `deps.sendExportEvent`. Bootstrap passes. OK.

Potential issue: The IPC handler `corpusSession` is under `export_` but `ApiHandlers['export_']` shape includes it. API_SURFACE updated. Good.

Potential issue: `CorpusExtractor.ts` comment says "AI-03 落地——真回传消费/落盘随其工单" but now AI-03 done; comment should not say "随其工单"? It says "真回传消费/落盘随其工单" maybe "随 AI-03 落地" but the sentence "AI-03 落地——真回传消费/落盘随其工单" is generic. Fine.

Potential issue: `export_.ts` comment "C-02 exportTo 同型——dialog 在 ipc 层；单飞判定在 service 单例，dialog 期间并发由模态性+BUSY 兜底". Good.

Potential issue: In `corpusSessionReqSchema`, `paperIds` optional but `strict()` means any extra keys rejected. OK.

Potential issue: `corpusSessionResSchema` `dir: z.string()` no min, fine.

Potential issue: `api-surface.ts` long line maybe lint max-len? If eslint max-len not enforced? Not in constitution. Could be NIT.

Potential issue: `corpus.export.service.ts` line count after diff maybe >500? Let's estimate. The file diff shows big addition 268 lines plus context. Original stub maybe ~150? Let's calculate approximate final line count: Original file had 111? The diff header says index 7e3078a..d35039f and changed 285? Work order says STUB→285 行实现, so final file 285 lines. Good under 500.

Potential issue: `tests/unit/services/corpus.export.test.ts` 374 lines under 500. Good.

Potential issue: `interface-template.ts` 47 lines.

Potential issue: The test imports `guardedDescribe` from '../../utils/guard'. If guard skip? No.

Potential issue: The test "正常全链" uses `expect(detail1).not.toBeNull()` then `detail1!`; linter no non-null? fine.

Potential issue: In test "落盘失败" `await h.svc.corpusItem({ ... fulltext })` after creating directory page-1.png. That fulltext call succeeds. Then figure call rejects. But `corpusItem` catch in service calls failSession and then rethrows. The `session` promise rejects with SessionError IO_ERROR. The figure `corpusItem` rejects with raw ENOTDIR. The test `await expect(session).rejects.toThrow()` passes. Good.

Potential issue: `failSession` calls `s.reject` after rm; if reject throws? no.

Potential issue: `exportCorpusSession` catch if `cleanRebuild` fails before `session=active`? session=active before try, so failSession works. Good.

Potential issue: At top of `exportCorpusSession`, if `input.paperIds` is undefined, it calls `listAllIds()` before setting session? It calls after busy check. Busy check first. OK.

Potential issue: `listAllIds()` can return huge list; schema max 1000 only when explicit, full library maybe >1000? No cap. Could be memory but local. OK.

Potential issue: `queue` stores full PaperDetail objects for all papers, memory okay.

Potential issue: `total: ids.length`, but if duplicate ids, duplicate outputs? If paperIds has duplicates, queue duplicate, manifest duplicate paper entries. Schema doesn't prevent. Could use Set to dedupe. User could send duplicate IDs; output manifest duplicate entries and overwrite same corpus md twice, fulltext same file twice, figures duplicated in list. Not severe but could be a correctness issue. Work order "paperIds" maybe should be distinct. Not specified. Maybe WARN: schema could `.refine` unique. But low.

Potential issue: `paperIds` order from input preserved; if explicit, manifest order = input order. If duplicates, weird. Not blocking.

Potential issue: In `orderAiNotes`, createdAt tiebreak uses string comparison? `a.createdAt < b.createdAt` lexicographic if ISO string, okay.

Potential issue: `ROLE_ORDER` `Record<AiNoteRole, number>`; if AiNoteRole includes 'adjudicate' etc. OK.

Potential issue: `questionOrder` if q is `'Q10'`? AiNoteQuestion Q1..Q7 only. Number('10') =10. Fine.

Potential issue: `interface-template.ts` static template "schemaVersion=1; 新增字段必须可选" But manifest schemaVersion is number; no versioning in code. OK.

Potential issue: The `INTERFACE_MD` also has "「manifest.json 存在」= 导出完整就绪（唯一激活判据）" but if export finished with errors, manifest exists; "完整" perhaps still ready but incomplete. Not issue.

Potential issue: In `corpus.export.service.ts` finalizing manifest includes `errors` only if `s.errors.length > 0`; interface-template says optional errors. OK.

Potential issue: Manifest `file: corpus/${paperId}.md` uses relative path no leading `./`. Interface says `corpus/<paperId>.md`. Good.

Potential issue: `fulltext` page order: The extractor sends fulltext payload per page; service pushes in receive order. If renderer misorders, fulltext order wrong. But AI-02 extractor likely sends page order. Not diff.

Potential issue: `figures` manifest list includes `figures/<paperId>/<name>`; interface says `figures/<paperId>/...`. Good.

Potential issue: The "清空重建" removes `manifest.json` and tmp, and subdirs; but if `dir` doesn't exist? cleanRebuild: rm force, mkdir recursive for subdirs, write INTERFACE.md at root. If parent doesn't exist, `rm(join(dir,'manifest.json'), force)` on nonexistent dir? `rm` with force true does not error if path does not exist? Node fs.rm with force true does not error if missing. But if parent path doesn't exist, `rm` on file path under missing parent with force true? Node's rm may throw ENOENT even with force? According docs, `force` when true, if path does not exist, no error. For file under nonexistent parent, it considers path missing, so no error? I think yes. Then `rm(join(dir, sub), {recursive:true, force:true})` on missing parent, no error. Then `mkdir(join(dir, sub), {recursive:true})` creates parents. Then `writeFile(join(dir,'INTERFACE.md'), ...)` after mkdir sub? Wait they create sub dirs `corpus`, `fulltext`, `figures`, but they don't create `dir` itself before writing INTERFACE.md at root. Order: for loop for sub: `await rm(join(dir, sub), ...); await mkdir(join(dir, sub), { recursive: true })`. The second mkdir with recursive true will create `dir` as parent. So after loop, dir exists. Then write INTERFACE.md. Good.

Potential issue: If `dir` is root of drive? User could pick C:\? pickFolder maybe. cleanRebuild would rm `C:\corpus` recursively? Actually join(dir,'corpus') only. It won't rm C:\. But `rm(join(dir,'corpus'), recursive)`. OK.

Potential issue: `mkdir(join(dir, sub), { recursive: true })` after rm; if dir is a file? pickFolder returns dir, but user could select file? OS dialog ensures dir. OK.

Potential issue: `exportCorpusSession` doesn't validate `dir` is absolute or exists. Main dialog returns absolute. Tests pass absolute. OK.

Potential issue: `pickFolder()` might return a path with symlinks; `cleanRebuild` could follow symlinks? `rm(join(dir, sub))` where `sub` is symlink to another directory: `rm` with recursive on symlink? Node rm on symlink to dir removes the symlink, not target? I think fs.rm on symlink removes symlink, not target. `mkdir` then creates real dir. Symlink attacks less relevant local. Could be security if export dir contains symlink `corpus` pointing outside; cleanRebuild would remove symlink only? Actually `fs.rm` on a symlink to directory with recursive true removes the symlink, not target per Node docs? It may follow? Let's recall: `fs.rm` with `recursive` on symlink: it removes symlink itself. So safe.

Potential issue: `writeFile` to `join(dir,'corpus',id+'.md')` if `corpus` was symlink created by user after clean? Not.

Potential issue: `fileStore.resolveManagedPath` could return path outside managed? It's main service, trusted.

Potential issue: `sessionId` random uses `now()` + Math.random; enough.

Potential issue: In `failSession`, reject with `new SessionError('IO_ERROR', message)`; but `session` promise may have been already rejected by another path? Could double reject no effect. OK.

Potential issue: `corpusItem` when `req.kind` is `'error'` and `finishPaper` ok false calls advance; if error reason? Manifest errors. OK.

Potential issue: `corpusItem` when `req.kind` not one of fulltext/figure/complete/error? It goes to else as error. Schema likely union, so fine.

Potential issue: `fulltext` chunks: `cur.fulltext.push(req.payload)` but doesn't associate page; if receive out of order, wrong. Could use page index. Not diff.

Potential issue: In test "corpus md golden" expected md computed after service export; but if `assembleCorpusMd` is non-deterministic? It sorts. OK.

Potential issue: "golden 逐字节 =assembleCorpusMd 输出" test doesn't verify `front-matter` exact; but since expected from same function, OK.

Potential issue: Does `assembleCorpusMd` include `exportedAt`? Test expected no exportedAt? It doesn't assert. But if both function and service include? Service uses function so same. The invariants says front-matter no exportedAt. Could test assert not contain 'exportedAt'? Not present. But existing assemble tests likely. Not issue.

Potential issue: `corpus.assemble.ts` orderAiNotes uses `sort` mutating copy, OK.

Potential issue: Use of `readFile` to check file existence is a race: after check, file could be deleted before extractor request; renderer fails. OK.

Potential issue: `readFile(managedPath)` reads full file just to check existence; for large PDFs this is expensive! It reads entire PDF into memory and discards, to check existence. Work order notes "fileExists 判定=resolveManagedPath+readFile 探测（fileStore 无 stat API——探测读取既有方法）". This is a performance issue: reading entire PDF bytes just to see if exists; could be hundreds MB. Better if FileStore had stat or use fs.open with 'r' (open handle without reading). Actually `readFile` loads full content. They could use `open(managedPath, 'r')` and close, or `access`. This is WARN: production quality/performance. But work order explicitly acknowledges no stat API, but using `readFile` to probe is heavy. Could use `open` from fs/promises without reading. `fileStore` could expose stat. Not blocking. Maybe NIT/WARN. Since local PDFs can be large, this is a real inefficiency. But not correctness.

Potential issue: The file existence check uses `await readFile(managedPath).catch(() => { fileExists = false })`; if file exists but too large, memory. WARN.

Potential issue: The source PDF may be a non-PDF file with `%PDF`? Not cared.

Potential issue: In `preparing`, for each id, they call `assembleCorpusMd` and write md, then push detail. If write fails, catch failSession. Good.

Potential issue: If `assembleCorpusMd` throws for one paper (e.g., malformed annotation), entire session fails? It is inside try; yes any malicious data aborts session. Could perhaps continue errors, but not specified. Work order says file missing continues; other errors maybe fail. Not issue.

Potential issue: `deps.repos.papers.detailById(id)` may throw? no.

Potential issue: `deps.repos.annotations.listByPaper(id)` may return rects; `startPaper` maps `rects: a.rects`; if a.rects is JSON string? repo returns parsed? likely.

Potential issue: `corpusItem` figure payload `Buffer.from(req.payload,'base64')` no validation of `page` number. If page is `1.5`, filename `page-1.5.png`; schema likely `z.number().int()`. OK.

Potential issue: The service doesn't have a `cancel` path. Not in scope.

Potential issue: In `bootstrap.ts`, `sendExportEvent` broadcasts to all windows but not if no window. OK.

Potential issue: `EVENT_CHANNELS.exportCorpus` existing from AI-02. Not in diff.

Potential issue: `schemas.ts` new `ExportCorpusEvent` type is `ExtractRequestEvent | ExportProgressEvent`, but event type literal includes `'extract-request'` and progress `.type='progress'`. OK.

Potential issue: `corpusSessionResSchema` lacks exportedAt; no.

Potential issue: The service final manifest includes `schemaVersion: 1` literal but no constant shared with INTERFACE. Could drift. Not issue.

Potential issue: In `interface-template.ts`, if the unescaped bug is fixed, the line `+> 本目录由 Synapse 应用导出（五件套：本文件+manifest.json+corpus/+fulltext/` no issue.

Potential issue: "受锁文件（tests/shared/migrations/CI/lint/构建/测试配置/脚本）变更必须带 [locked-change] 尾注" The commit message not shown. Can't judge.

Potential issue: "禁止修改 tests/**、**/*.test.ts" They added new test, not modified existing. But adding test file to `tests/` is allowed? "禁止修改 tests/**" literally includes adding? The rule says "禁止修改 tests/**、**/*.test.ts" (CI sha256 对账). Adding new test file is a change to tests/ (new file) but lock manifest tracks it. In work order, tests are "受锁新". Existing test files are locked; new test file is added with manifest. Does "禁止修改 tests/**" prohibit adding? The intent is don't alter tests to make code pass; adding new tests is normal. The locks workflow allows adding locked files via locks:generate. So OK.

Potential issue: `locks/manifest.json` added new test path with sha, but `src/shared/ipc/schemas.ts` sha changed; app-error sha changed; api-surface sha changed. All good. Did they add `interface-template` to locks? Not locked. No.

Potential issue: In `tests/unit/services/corpus.export.test.ts`, they use `fs.rm` via dynamic import to dispose; could import at top. Not issue.

Potential issue: The test uses `h.db.prepare('INSERT INTO papers ...')` directly, bypassing repos, but OK for fixture.

Potential issue: The test with `seedAiNote` calls `h.repos.aiNotes.insert` ignoring annotationId null; if schema requires anchor fields etc. fine.

Potential issue: `orderAiNotes` uses `n.model` as source. If model contains spaces? no.

Potential issue: `content: \`${n.question}: ${n.contentMd}\`` could conflict with output if contentMd has newline. Not.

Potential issue: In `interface-template.ts`, "corpus md 结构" says "引文块：`> 引文原文（p.N）`——N 为 1 基页码（存储 0 基，显示 1 基）" good.

Potential issue: `INTERFACE_MD` static template claims "2 倍分辨率渲染" but figure rendering maybe not 2x? Not in diff, okay.

Now, let's decide findings list and severities.

We should include:
1. BLOCKING: interface-template.ts unescaped `${paperId}` and `${annotationId}` in template literal -> ReferenceError/TS compile fail. This is a contract/correctness issue. Evidence: diff lines. Suggestion: escape as `\${paperId}`, `\${annotationId}` or build string with string concat.
2. BLOCKING: path traversal via `annotationId` in corpusItem figure filenames. The `annotationId` from renderer IPC is interpolated into a path segment without sanitization, allowing `..` to escape `figures/<paperId>` (and with enough `..`, outside export dir). Evidence: `name = req.figure === 'anno' ? \`anno-${req.annotationId ?? 'unknown'}.png\` : \`page-${req.page}.png\`` and `join(figDir, name)`, `writeFile`. Suggestion: validate annotationId against known annotation ids or use regex allowlist (e.g., `^[A-Za-z0-9_-]+$`), or use `path.basename`/sanitize; better use annotation id from extracted request list, not arbitrary.
3. WARN: performance issue check file existence by readFile entire file. Suggestion use `open` handle or `stat` if add API, or FileStore stat.
4. WARN: stale session cleanup race: failSession sets session null before rm MANIFEST_TMP; a new session could have its tmp deleted. Suggestion unique tmp per session or keep busy until cleanup complete, or remove before release.
5. WARN/NIT: `created as unknown as ActiveSession` type assertion smell; also `let created` null cast. Maybe NIT.
6. WARN/NIT: `interface-template` line with `${annotationId}` is same as #1 but also mention.
7. NIT: `corpusSessionReqSchema` comment says directory "service 内选" but actual selection in ipc layer. Actually comment in schemas says "目录经 main 侧系统对话框（INV-07）service 内选" maybe means "service in main" not "service layer"? Chinese "main 侧系统对话框（INV-07）service 内选" might be read as "目录经 main 侧系统对话框 ... service 内选" Hmm "service 内选" likely "service layer around main select"? Let's parse: `// 会话发起：paperIds 缺省=全库；目录经 main 侧系统对话框（INV-07）service 内选` Could mean "目录经 main 侧系统对话框（INV-07）[在] service 内选" or "service 内选" contradictory. The code has dialog in ipc, service receives dir. The comment "service 内选" is inaccurate. NIT.
8. NIT: left-over STUB instruction line in corpus.export.service header "完成后：删除 STUB" after done. Minor.

Need be careful not to over-flag. The prompt wants first-pass audit. We should output findings maybe 5-7.

Need ensure no fabricated evidence. The unescaped template literal is in diff. We'll cite exact diff line `+- \`corpus/<paperId>.md\`：每篇语料...` and `+- \`figures/<paperId>/anno-<annotationId>.png\`...`; indicate these are inside backtick template literal. Also note `export const INTERFACE_MD = \`...\`` from diff. Good.

Path traversal evidence: diff in corpus.export.service.ts:
```
+          const name =
+            req.figure === 'anno'
+              ? `anno-${req.annotationId ?? 'unknown'}.png`
+              : `page-${req.page}.png`
+          await writeFile(join(figDir, name), Buffer.from(req.payload, 'base64'))
```
This is in diff. We can mention `annotationId` comes from `CorpusItemReq`, renderer-controlled, only checked for session/paper mismatch, no sanitize. Good.

Wait, the schema for corpusItemReq from AI-02 maybe annotationId is in an allowed list? The code doesn't enforce. But the renderer is our own app, not malicious. However security constitution says "禁止 renderer 引入任何 Node/Electron API 或绝对文件路径（路径只能来自 main 侧系统对话框）". It doesn't say renderer can't provide filenames. Still path traversal is a real vuln if renderer compromised or if future. Since this is local single-user app, threat model maybe less. But audit dimension lists path traversal as security. BLOCKING? The AGENTS hard rules security say "禁止 renderer 引入任何 Node/Electron API 或绝对文件路径（路径只能来自 main 侧系统对话框）" The renderer provides annotationId, not absolute path, but can cause path traversal. Security rule not explicit. Still BLOCKING due to path traversal.

Could `annotationId` be sanitized by schema? Not in diff. In test, `CorpusItemReq` type from schemas. Need know if `annotationId` maybe `z.string().regex(/^...$/)`. The diff for schemas doesn't show corpusItemReqSchema modification. It existed from AI-02 and is locked; if it already had pattern, then code safe. The diff only adds corpusSessionReq/Res. The existing `corpusItemReqSchema` not shown. The prompt says "schemas 增 corpusSessionReqSchema/ResSchema" only. The service diff uses `annotationId` from `CorpusItemReq`. If existing schema had `annotationId: z.string().uuid()`? We don't know. Work order maybe annotations IDs are uuid. The code doesn't rely on schema? It is not visible. Audit should not assume absent validation? But the service code should defend even if schema? The schema is renderer input validation in shared; if it prohibits `..`, then path traversal blocked. But we don't have evidence of that. The diff doesn't show corpusItemReqSchema; cannot claim no validation. But we can say "若 corpusItemReqSchema 未对 annotationId 做字符集限制，则..." but audit should be decisive. The instruction says don't fabricate diff content. We can't assume schema lacks pattern. But the service itself constructs path from annotationId; even if schema currently allows IDs generated by app (uuids), output still should validate to avoid future. But if we mark BLOCKING, need evidence. The schema in shared is locked and not shown; perhaps existing schema already has `annotationId: z.string().uuid()` because AI-02. Let's think: Annotation ids likely strings from DB, maybe UUIDs. The extractor sends annotation IDs it got from request annotations mapping, not arbitrary. The schema might just `z.string()`. Hmm.

Given first-pass audit, path traversal from IPC input is a classic security issue worth WARN/BLOCKING. But if the schema is already strict, this is mitigated. However the audit output should be based on provided diff; we can note "no allowlist/character check visible at the point of path construction." Is that a violation? I'd mark BLOCKING because security path traversal. But maybe too harsh if existing schema. Could use WARN to be safe? The requirement: BLOCKING=安全问题. A path traversal is security. I'll mark BLOCKING with caveat "若上层 schema 未限制 annotationId 字符集" not ideal. Better: "不存在针对 annotationId 的清洗；即使上层 schema 宽松也应由 service 防御." Since security should be defense in depth.

Let's also consider `paperId` in `exportCorpusSession` could path traverse if DB contains malicious id. But IDs are internal. AnnotationId is renderer input in corpusItem and only checked equal current.paperId, not annotation. So highlight.

Potential issue: In `corpusItem`, `req.figure === 'anno'` branch uses `req.annotationId ?? 'unknown'`; if `annotationId` is empty string, filename `anno-.png`; if undefined unknown. Not path traversal. If schema allows slashes, path.

Potential issue: The session `dir` is from main dialog; however `annotationId` path traversal could write outside export dir, which is not selected by user for that file? It is inside/around dir. User chose dir; writing outside dir would violate. So security.

Potential issue: `exportCorpusSession` `paperIds` input may be used in errors and paths after detail exists; if paper ID has slash and row exists, path traversal. If DB ids are user-controlled? Maybe import service uses UUID. Not.

Potential issue: Also `req.page` if schema is number; if number, safe. If string, path traversal via `page-${req.page}.png`: `page=../../x` -> name `page-../../x.png` might escape? But if schema z.number, no. Not visible.

Now see if any rule "禁止新增出网 host" no network.

Test discipline: Did they weaken existing tests? No. New test lock manifest added. But one thing: The new test "normal chain" `expectedMd = assembleCorpusMd(...)` same function; not a golden fixture. Work order called "golden 逐字节=assembleCorpusMd output". It's a tautological test for content. But the test also checks `md1` contains `[user]` and `[ai:glm-5.3]`, so some content anchored. However if assembleCorpusMd itself is buggy, test won't catch. But assemble already has tests? Probably. Not a test discipline violation. The invariant says "测试是锁定的合约"; no relaxation.

Could mention existing tests not modified. Good.

Potential issue: `npm run verify` green with 69 files 409 cases; but the interface-template bug would break typecheck. So either brief false or our reading wrong. If the actual source has `\${`, then no bug. We should be certain before outputting BLOCKING. Let's revisit the diff's raw representation. The user typed:

```
+export const INTERFACE_MD = `# Synapse 语料导出接口说明书（INTERFACE）
+
+> 本目录由 Synapse 应用导出（五件套：本文件+manifest.json+corpus/+fulltext/
+> figures/）。「manifest.json 存在」= 导出完整就绪（唯一激活判据）；导出中断
+> = 无 manifest，重跑导出即修复（目录清空重建）。
+
+## 目录结构
+
+- \`INTERFACE.md\`：本说明书
+- \`manifest.json\`：导出索引（schemaVersion/exportedAt/papers[]/可选 errors[]；
+  contentSha/fulltextSha=对应文件字节 sha256，供增量对比）
+- \`corpus/<paperId>.md\`：每篇语料（结构化标注+笔记，二阶消费主面）
+- \`fulltext/<paperId>.txt\`：全文本层（pdf 文本按页拼接，**页界符 \\f 分隔**，
+  页序 1 基——一阶消费主面）
+- \`figures/<paperId>/page-<N>.png\`：页级快照（N=1 基页码；2 倍分辨率渲染）
+- \`figures/<paperId>/anno-<annotationId>.png\`：标注区域裁剪（归一化 rects
+  包围盒从页快照裁出，与 corpus md 引文块对应）
```

Let's see if the backslash before backtick is also in lines. In the raw source, to include literal backtick in template, they need `\``. The prompt shows that. There is no `\` before `$`. Therefore TypeScript would interpolate. However, what about the line `+- \`corpus/<paperId>.md\`：...`: The first backslash before the backtick is an escape for the backtick; then `corpus/<paperId>.md`; the `${` starts an expression. No escape. Yes.

Would TS compile error "Cannot find name 'paperId'"? Yes. This would make typecheck fail. The brief says typecheck passed. Maybe the diff is intentionally malicious? The audit should catch.

But let's also consider: In TypeScript, template literal expressions in a module with an undeclared variable cause error TS2304. Unless `paperId` is declared in module? It is not. So no.

Could it be that this file is not actually TypeScript but a `.ts` with `// @ts-nocheck`? No.

Thus verdict likely FAIL.

Now, consider if path traversal should be BLOCKING and if unescaped template literal is enough. We can output findings with both.

Need decide summary.

Let's draft JSON:

{
  "verdict": "FAIL",
  "findings": [
    {
      "severity": "BLOCKING",
      "file": "src/main/services/export_/interface-template.ts",
      "line_hint": "INTERFACE_MD 模板内 `corpus/<paperId>.md` / `anno-<annotationId>.png`",
      "issue": "模板字符串中 `${paperId}`/`${annotationId}` 未转义，模块加载即 ReferenceError（typecheck 也会报 TS2304），五件套导出不可用。",
      "evidence": "diff: `export const INTERFACE_MD = \\`...\\`` 内 `- \\`corpus/<paperId>.md\\`：...`、`- \\`figures/<paperId>/anno-<annotationId>.png\\`：...`",
      "suggestion": "改为 `\\${paperId}`、`\\${annotationId}`，或用普通字符串拼接；添加断言测试确保 INTERFACE.md 含字面量 `${...}`。"
    },
    {
      "severity": "BLOCKING",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "corpusItem figure 分支：`name = ... anno-${req.annotationId ?? 'unknown'}.png` + `join(figDir, name)` + `writeFile`",
      "issue": "annotationId 来自 renderer IPC 载荷，未做字符集/路径段校验即拼入文件名；含 `../` 时可通过 path.join 越出 figures/<paperId> 甚至导出目录，构成路径穿越写文件。",
      "evidence": "diff: `const name = req.figure === 'anno' ? \\`anno-${req.annotationId ?? 'unknown'}.png\\` : \\`page-${req.page}.png\\`; await writeFile(join(figDir, name), Buffer.from(req.payload, 'base64'))`",
      "suggestion": "service 侧对 annotationId 做 allowlist 校验（如 `^[A-Za-z0-9_-]+$`），或与当前论文的已知 annotation id 集合比对；使用 path.basename 不足以防御，因为前缀 `anno-` 拼接后仍可带 `..`。"
    },
    {
      "severity": "WARN",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "preparing 文件存在性判定：`await readFile(managedPath).catch(...)`",
      "issue": "用 readFile 整个文件来探测存在性，会把大 PDF 全量读入内存再丢弃，导出准备阶段性能/内存开销大。",
      "evidence": "diff: `const managedPath = deps.fileStore.resolveManagedPath(detail.fileName); let fileExists = true; await readFile(managedPath).catch(...)`",
      "suggestion": "给 FileStore 增加 stat/open 判定（或直接 `open(managedPath, 'r')` 后 close），避免读取文件内容。"
    },
    {
      "severity": "WARN",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "failSession：`session = null` 先于 `await rm(MANIFEST_TMP)`",
      "issue": "失败清理期间已释放单飞锁且异步删除 manifest.tmp；若用户立刻重跑同一目录，新会话 finalizing 写的 manifest.tmp 可能被旧会话未完成的 rm 删掉，导致新会话误失败。",
      "evidence": "diff: `async function failSession(s, message) { session = null; await rm(join(s.dir, MANIFEST_TMP), { force: true }).catch(...)`",
      "suggestion": "先完成 tmp 清理再释放 session（或使用含 sessionId 的唯一 tmp 名并清理旧名），确保新旧会话无交叉删除。"
    },
    {
      "severity": "NIT",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "`const active = created as unknown as ActiveSession`",
      "issue": "用 `as unknown as` 绕过类型，可读性差；实际可在 Promise executor 内直接给非空局部变量赋值。",
      "evidence": "diff: `let created: ActiveSession | null = null; const promise = new Promise<CorpusSessionRes>((resolve, reject) => { created = {...} }); const active = created as unknown as ActiveSession`",
      "suggestion": "改为 `let active: ActiveSession; new Promise<CorpusSessionRes>((resolve, reject) => { active = {...}; })` 或使用 `!` 断言并提供注释。"
    },
    {
      "severity": "NIT",
      "file": "src/shared/ipc/schemas.ts",
      "line_hint": "corpusSessionReqSchema 上方注释",
      "issue": "注释称“目录…service 内选”，实际目录选择在 main/ipc 层，service 只接收 dir，口径不一致。",
      "evidence": "diff: `// 会话发起：paperIds 缺省=全库；目录经 main 侧系统对话框（INV-07）service 内选`；实测 ipc/export_.ts 中 `const dir = await deps.dialogs.pickFolder()`",
      "suggestion": "改为“对话框在 ipc 层，service 接收已选目录”。"
    }
  ],
  "summary": "实现主体与状态机/测试设计基本对齐，但存在两处 BLOCKING：INTERFACE_MD 模板未转义 `${...}` 会导致模块加载/类型检查失败，以及 annotationId 未经清洗拼路径的路径穿越风险；另外文件存在性探测与失败清理存在性能和竞态隐患。"
}

Need ensure verdict FAIL due BLOCKING. If the template bug were a representation artifact, finding would be false. But audit should trust input. The path traversal should be blocking. If we want to be more conservative, maybe mark path traversal as WARN because local app and schema may constrain. But output with BLOCKING is acceptable for security.

Let's also consider there may be a third BLOCKING: `interface-template.ts` has "figures/<paperId>/anno-<annotationId>.png" etc. The unescaped template bug affects the file import and calls into question "verify passed". We can say "typecheck would fail" maybe not if runtime only? Actually TS compile catches. So the code as shown cannot have passed verify. This also violates "完成定义" (verify green). Good.

Could there be issue with "src/main/services/export_/corpus.export.service.ts" `SessionError` code `'IO_ERROR'` not in AppErrorCode? Let's verify from app-error diff. The code type at line 22 context:
```
export type AppErrorCode =
  | 'UPSTREAM_ERROR'
  | 'PARSE_ERROR'
  | 'CANCELLED'
+ | 'EXPORT_BUSY'
  | 'INTERNAL'
```
What are codes before? Maybe `'NOT_FOUND'`, `'IO_ERROR'`, `'DB_ERROR'`, etc. The diff before line 22 is not shown. The export.service uses `ExportDomainError` maybe code `'CONFLICT'`. So `IO_ERROR` likely exists. Not flag.

Let's think if the unescaped template literal line in diff could be escaped by the plus sign? No.

Let's maybe add a NIT: stale "完成后：删除 STUB" comment. But not necessary; we already have 6 findings. Too many? The prompt says findings all. Could include. But we can keep concise.

Potential issue: `corpus.export.service.ts` header mentions "完成后：删除 STUB" after implementation. This is stale and could confuse. It's not a placeholder? It literally says "完成后：删除 STUB". Since STUB already deleted, it's an outdated instruction. Could be NIT. But we already have NITs. Not necessary.

Potential issue: In test, `dispose` uses dynamic import; could import `rm` top-level. Not issue.

Potential issue: In `locks/manifest.json`, they added new test file sha but not `interface-template`, but `interface-template` is not locked. OK.

Potential issue: The brief says "diff 797 行，15 文件" but diff actually 15 files? Count: invariants, locks, bootstrap, ipc/export, corpus.assemble, corpus.export, export.service, interface-template, services/index, CorpusExtractor, app-error, api-surface, schemas, test, registry = 15. Good.

Potential issue: "check-tickets 短形化三处全形引用" in brief: they changed "SR2-AI-03" references in app-error/interface-template/CorpusExtractor comments to short "AI-03"? In diff, app-error comment "AI-03 落地"; CorpusExtractor "AI-03 落地"; but interface-template header "AI-03 交付面" not short? Actually "AI-03" short. The brief says "check-tickets 短形化三处全形引用（app-error/interface-template/CorpusExtractor 头注——AI-03 done 后引用须短形）" In diff, app-error code comment `'EXPORT_BUSY' // 导出会话单飞：进行中拒绝第二会话（INV-18，AI-03 落地）` yes short. interface-template header says "AI-03 交付面" short. CorpusExtractor comment "AI-03 落地" short. Good.

Potential issue: `corpus.export.service.ts` header still uses "SR2-AI-03" in "work order" comment? The file's header says `(AI-03 实现前占位形态)` removed? It has "AI-03 延展" etc. It also has "SR2-AI-03 实现前占位形态" removed. The remaining "AI-03" short. Registry done. Good.

Potential issue: `corpus.export.service.ts` header at end "完成后：删除 STUB → npm run verify 绿 → 人工审查 git diff → 翻 registry" remains. This might be a stale instruction but not ticket reference. NIT.

Potential issue: In interface-template.ts, the module is new; if the template literal bug were fixed, the file is 47 lines. Fine.

Potential issue: Path traversal: also `figDir = join(s.dir, 'figures', cur.paperId)` uses `cur.paperId` from DB. If DB paperId not safe, same. But less.

Potential issue: Could `req.annotationId` be from schema with `z.string().regex(/^[a-zA-Z0-9-]+$/i)`? Not in diff. But the service comment "annotations 裁剪数据随发" from existing schemas; annotationId likely a database id. Existing app uses uuid? If so path traversal not possible in normal use. But defense-in-depth still. To avoid false positive, we can phrase "未在 service 层校验；即便 schema 未限定字符集" with evidence from diff. The expected audit should catch security issues even if app not malicious.

Potential issue: "annotationId" from renderer could be undefined for anno? The extractor sends annotationId from `req.annotations` mapping, so defined. No.

Potential issue: There is also a path traversal with `req.page` if `page` is a number but could be `Infinity`? `z.number()` includes finite? Not sure. If page is huge, filename. Not.

Potential issue: `Buffer.from(req.payload, 'base64')` may decode to arbitrary bytes; if file name escapes, writes arbitrary content. Good.

Potential issue: The file existence check could also be a security issue if `resolveManagedPath` returns a symlink? no.

Let's decide if path traversal severity should be BLOCKING or WARN. The user's grading says BLOCKING=违反硬规则/正确性缺陷/安全问题. So any security = BLOCKING. We'll mark BLOCKING.

Need ensure JSON valid. Use double quotes, escape internal double quotes. We'll write no markdown fences.

Let's craft final JSON carefully. The `evidence` strings should be from input, but we can paraphrase. Use "diff:" quotes. Need escape backticks? In JSON double quotes no issue. Backslashes need escape. For evidence with backtick and `${}`, we need escape backslash? JSON string can contain backslash only if escaping. If we write "`anno-${req.annotationId}`" no backslash. The `${}` is fine. Avoid backslashes. For template literal evidence, we can write "INTERFACE_MD = `...` 内 `corpus/<paperId>.md`" no need backslash. The actual diff had `\``, but we can ignore.

Let's produce.

Double-check summary: "实现主体与状态机/测试设计基本对齐" okay.

Potential finding: Should we include "locks/manifest.json" maybe no. Good.

Let's final.

== content ==
{
  "verdict": "FAIL",
  "findings": [
    {
      "severity": "BLOCKING",
      "file": "src/main/services/export_/interface-template.ts",
      "line_hint": "INTERFACE_MD 模板内 `corpus/<paperId>.md` / `anno-<annotationId>.png`",
      "issue": "模板字符串中 `${paperId}`、`${annotationId}` 未转义，模块加载即触发 ReferenceError（typecheck 也会报 TS2304），五件套导出链路不可用。",
      "evidence": "diff 中 `export const INTERFACE_MD = `# ...` 内部出现 `- \\`corpus/<paperId>.md\\`...`、`- \\`figures/<paperId>/anno-<annotationId>.png\\`...`，这些 `${...}` 会被当作模板插值。",
      "suggestion": "改为 `\\${paperId}`、`\\${annotationId}`，或用普通字符串拼接/数组 join；并加断言确保 INTERFACE.md 输出包含字面量 `${paperId}`/`${annotationId}`。"
    },
    {
      "severity": "BLOCKING",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "corpusItem figure 分支：`name = anno-${req.annotationId ?? 'unknown'}.png` + `join(figDir, name)` + `writeFile`",
      "issue": "annotationId 来自 renderer IPC 载荷，未做字符集/路径段校验即拼入文件名；含 `../` 时可通过 path.join 越出 figures/<paperId> 甚至导出目录，构成路径穿越写文件。",
      "evidence": "diff: `const name = req.figure === 'anno' ? \\`anno-${req.annotationId ?? 'unknown'}.png\\` : \\`page-${req.page}.png\\`; await writeFile(join(figDir, name), Buffer.from(req.payload, 'base64'))`",
      "suggestion": "service 侧对 annotationId 做 allowlist 校验（如 `^[A-Za-z0-9_-]+$`），或与当前论文已知 annotation id 集合比对后再拼路径。"
    },
    {
      "severity": "WARN",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "preparing 文件存在性判定：`await readFile(managedPath).catch(...)`",
      "issue": "用 readFile 整个文件来探测存在性，会把大 PDF 全量读入内存再丢弃，导出准备阶段性能/内存开销大。",
      "evidence": "diff: `const managedPath = deps.fileStore.resolveManagedPath(detail.fileName); let fileExists = true; await readFile(managedPath).catch(() => { fileExists = false; })`",
      "suggestion": "给 FileStore 增加 stat/open 判定，或直接用 `open(managedPath, 'r')` 后 close，避免读取文件内容。"
    },
    {
      "severity": "WARN",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "failSession：`session = null` 先于 `await rm(MANIFEST_TMP)`",
      "issue": "失败清理期间已释放单飞锁且异步删除 manifest.tmp；若用户立刻重跑同一目录，新会话 finalizing 写入的 manifest.tmp 可能被旧会话未完成的 rm 删除，导致新会话误失败。",
      "evidence": "diff: `async function failSession(s, message) { session = null; await rm(join(s.dir, MANIFEST_TMP), { force: true }).catch(...)`",
      "suggestion": "先完成 tmp 清理再释放 session，或使用含 sessionId 的唯一 tmp 文件名并清理旧名，确保新旧会话无交叉删除。"
    },
    {
      "severity": "NIT",
      "file": "src/main/services/export_/corpus.export.service.ts",
      "line_hint": "`const active = created as unknown as ActiveSession`",
      "issue": "用 `as unknown as` 绕过类型系统，可读性差；实际可在 Promise executor 内给非空局部变量赋值。",
      "evidence": "diff: `let created: ActiveSession | null = null; const promise = new Promise<CorpusSessionRes>((resolve, reject) => { created = {...} }); const active = created as unknown as ActiveSession`",
      "suggestion": "改为 `let active: ActiveSession; new Promise<CorpusSessionRes>((resolve, reject) => { active = {...}; })`，减少双重断言。"
    },
    {
      "severity": "NIT",
      "file": "src/shared/ipc/schemas.ts",
      "line_hint": "corpusSessionReqSchema 上方注释",
      "issue": "注释称“目录…service 内选”，实际目录选择在 main/ipc 层，service 只接收已选目录，口径不一致。",
      "evidence": "diff: `// 会话发起：paperIds 缺省=全库；目录经 main 侧系统对话框（INV-07）service 内选`；而 ipc/export_.ts 中 `const dir = await deps.dialogs.pickFolder()`",
      "suggestion": "改为“对话框在 ipc 层，service 接收已选目录”。"
    }
  ],
  "summary": "实现主体与状态机/测试设计基本对齐，但存在两处 BLOCKING：INTERFACE_MD 模板未转义 `${...}` 会导致模块加载/类型检查失败，以及 annotationId 未清洗拼路径的路径穿越风险；另有文件存在性探测性能与失败清理竞态隐患需加固。"
}